name = "Boundary"
uuid = "d6ef1b8b-8768-406c-8849-246498603918"
version = "0.0.1"

# The package a Julia user installs. It carries no binding dependencies: the
# declarations that expose it to other languages live in `lib/`, which depends
# on this project rather than the other way round.

[compat]
julia = "1.13"
