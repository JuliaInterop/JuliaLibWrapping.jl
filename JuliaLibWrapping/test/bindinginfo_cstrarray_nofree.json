{
  "_comment": "Task 9 fixture: identical to bindinginfo_cstrarray.json EXCEPT the two jlw_free*  release-entrypoint function entries are deleted (the `types` array — including the synthetic `Nothing`/`Ptr{Nothing}` pair those functions used — is left untouched, per the task-9 brief's literal instruction). This models a library that defines the CStrArray carrier but forgot (or has not yet called) `JLWInterop.@export_release_entrypoints`. `_release_symbols_present` must see that `jlw_free`/`jlw_free_strings` are absent from `functions` and gate the owning return `give_strs` back to `:mechanical` with a TODO naming `JLWInterop.@export_release_entrypoints`, while the borrow-in `take_strs` stays `:auto` (arg-side classification is unaffected by the gate).",
  "types": [
    {
      "kind": "primitive",
      "name": "UInt8",
      "id": 1,
      "size": 1,
      "bits": 8,
      "signed": false,
      "alignment": 1
    },
    {
      "kind": "primitive",
      "name": "Int64",
      "id": 2,
      "size": 8,
      "bits": 64,
      "signed": true,
      "alignment": 8
    },
    {
      "kind": "pointer",
      "name": "Ptr{UInt8}",
      "id": 3,
      "pointee_type_id": 1
    },
    {
      "kind": "pointer",
      "name": "Ptr{Ptr{UInt8}}",
      "id": 4,
      "pointee_type_id": 3
    },
    {
      "kind": "struct",
      "name": "CStrArray",
      "id": 5,
      "size": 16,
      "fields": [
        { "name": "length", "type_id": 2, "offset": 0, "isptr": false, "isfieldatomic": false },
        { "name": "data", "type_id": 4, "offset": 8, "isptr": false, "isfieldatomic": false }
      ],
      "alignment": 8
    },
    {
      "kind": "struct",
      "name": "Nothing",
      "id": 6,
      "size": 0,
      "fields": [],
      "alignment": 1
    },
    {
      "kind": "pointer",
      "name": "Ptr{Nothing}",
      "id": 7,
      "pointee_type_id": 6
    }
  ],
  "functions": [
    {
      "returns": { "type_id": 2 },
      "name": "take_strs(a::CStrArray)",
      "arguments": [
        { "name": "a", "type_id": 5 }
      ],
      "symbol": "take_strs"
    },
    {
      "returns": { "type_id": 5 },
      "name": "give_strs()",
      "arguments": [],
      "symbol": "give_strs"
    }
  ]
}
