var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"constant","location":"api/#JuliaLibWrapping._RELEASE_ENTRYPOINT_SYMBOLS","page":"API reference","text":"_RELEASE_ENTRYPOINT_SYMBOLS :: NTuple{2, String}\n\nRelease symbols emitted by JLWInterop.@export_release_entrypoints. Targets use them to manage owning carrier returns without exposing them as public API.\n\n\n\n\n\n","title":"JuliaLibWrapping._RELEASE_ENTRYPOINT_SYMBOLS"},{"category":"constant","location":"api/#JuliaLibWrapping.numpy_dtypes","page":"API reference","text":"numpy_dtypes :: Dict{String, String}\n\nMap from Julia primitive type name to the corresponding numpy dtype string. Used by the Python façade emitter to decide which pointer element types can be exposed as numpy arrays in CVector / CMatrix helpers; primitives absent from this table (notably Bool, the platform-aliased C ints) are not auto-wrapped.\n\n\n\n\n\n","title":"JuliaLibWrapping.numpy_dtypes"},{"category":"constant","location":"api/#JuliaLibWrapping.pytypes","page":"API reference","text":"pytypes :: Dict{String, String}\n\nMap from Julia primitive type name (as it appears in a PrimitiveTypeDesc's name field, e.g. \"Int64\", \"Float32\", \"Cvoid\") to the corresponding ctypes expression (e.g. \"ctypes.c_int64\", \"ctypes.c_float\", \"None\"). Used by mangle_python! for scalar/primitive arguments and returns, and by _python_carray_info for a CArray's element type.\n\n\n\n\n\n","title":"JuliaLibWrapping.pytypes"},{"category":"constant","location":"api/#JuliaLibWrapping.scalar_payload_types","page":"API reference","text":"scalar_payload_types :: Dict{String, String}\n\nThe allowlist of Julia scalar primitive types accepted as a CDict{V} value or COpt{T} payload — Int8..Int64, UInt8..UInt64, Float32, Float64, Bool — matching JLWInterop.CDICT_VALUE_TYPES exactly (CDict/COpt's payload is inline in the carrier struct/array, so they share one scalar contract; Bool is ctypes.c_bool, a real scalar here, even though it is intentionally absent from numpy_dtypes for the unrelated reason that numpy has no single-byte boolean ctypes array element convention this emitter wraps).\n\nThis is a restriction of pytypes, not a separate vocabulary: pytypes also maps several primitive names that are unsuitable as a CDict/COpt payload even though they are valid elsewhere (an argument, a return, a pointee) — \"Cvoid\" => \"None\" is not a legal ctypes.Structure field type (None cannot appear in _fields_); Cstring/Cwstring are pointer-backed nominal types needing their own lifetime management, not a value CDict/COpt's inline-scalar convention can hold; RawFD and the platform-aliased C ints (Cint, Cshort, …) are not part of CDICT_VALUE_TYPES either. Using pytypes for this purpose would silently accept CDict{Cvoid}/COpt{Cvoid} fixtures that cannot actually round-trip.\n\nUsed by _python_cdict_info/_python_copt_info instead of pytypes to decide whether a recognized CDict/COpt payload type is supported.\n\n\n\n\n\n","title":"JuliaLibWrapping.scalar_payload_types"},{"category":"type","location":"api/#JuliaLibWrapping.ABIInfo","page":"API reference","text":"ABIInfo\n\nFields\n\ntypeinfo: type descriptors in declaration order.\nforward_declared: type IDs requiring C forward declarations.\nentrypoints: exported functions.\n\n\n\n\n\n","title":"JuliaLibWrapping.ABIInfo"},{"category":"type","location":"api/#JuliaLibWrapping.AbstractTarget","page":"API reference","text":"AbstractTarget\n\nSupertype of wrapper-emission targets. Each concrete subtype is a configuration struct describing where and how to emit one output language's bindings; a corresponding write_wrapper method consumes that configuration plus an ABIInfo and writes the files.\n\nShips today: CTarget for a C header, PythonTarget for a Python ctypes package. New languages are added by defining a subtype and a write_wrapper method for it.\n\n\n\n\n\n","title":"JuliaLibWrapping.AbstractTarget"},{"category":"type","location":"api/#JuliaLibWrapping.CTarget","page":"API reference","text":"CTarget(dir, headerbase)\n\nOutput configuration for a C header. write_wrapper writes a single file joinpath(dir, headerbase * \".h\") containing typedefs for every struct in the ABI and extern declarations for every entrypoint.\n\nOnly the primitive Julia types listed in the emitter's ctypes table have a direct C mapping; an ABI referencing any other primitive raises an error. Pointer types are emitted inline as T* rather than as separate typedefs. Non-C-safe identifiers are scrubbed by sanitize_for_c and disambiguated with a numeric suffix on collision.\n\n\n\n\n\n","title":"JuliaLibWrapping.CTarget"},{"category":"type","location":"api/#JuliaLibWrapping.PythonTarget","page":"API reference","text":"PythonTarget(dir, package_name, library_basename;\n             bundle_subdir = nothing, version = \"0.0.0\",\n             privatized = false)\n\nOutput configuration for a Python ctypes-based wrapper package. dir is the directory into which the package will be written; a sub-directory named package_name is created and is the importable Python module. library_basename is the shared library's basename without an OS-specific suffix (e.g. \"libsimple\", which will be loaded from libsimple.so / libsimple.dylib / libsimple.dll depending on the host).\n\nWhen bundle_subdir is a string (e.g. \"bundle\"), the emitter assumes the shared library and its juliac runtime closure (libjulia, sysimage, stdlibs, artifacts) will be laid out under that subdirectory of the Python package in the standard --bundle shape (<bundle_subdir>/lib/<lib>, <bundle_subdir>/lib/julia/, <bundle_subdir>/artifacts/). The generated loader looks for the library inside the bundle first, the generated pyproject.toml widens package-data to include the bundle tree, and build_library with bundle = true will copy the bundle there. The default nothing preserves the flat single-.so-next-to-the-package layout and is the right choice for callers placing the library by hand.\n\nversion sets the version field in the generated pyproject.toml. It must be PEP 440-compatible; a Julia Major.Minor.Patch version string is valid.\n\nprivatized records whether the bundle carries a salted libjulia. A package without one warns if another wrapped package is already loaded. build_library sets this option; pass it directly only when using write_wrapper for a bundle built elsewhere.\n\n\n\n\n\n","title":"JuliaLibWrapping.PythonTarget"},{"category":"method","location":"api/#JuliaLibWrapping._api_kwarg_default_python-Tuple{Bool}","page":"API reference","text":"_api_kwarg_default_python(value) -> String\n\nWrite an @api keyword argument default, as it was parsed from the metadata sidecar's JSON, in Python literal syntax. A string is re-emitted through JSON.json, whose escapes are also valid Python. Any other value type is an error: the sidecar carries only numbers, strings, booleans and null.\n\n\n\n\n\n","title":"JuliaLibWrapping._api_kwarg_default_python"},{"category":"method","location":"api/#JuliaLibWrapping._carrier_ownership-Tuple{AbstractString, Any}","page":"API reference","text":"_carrier_ownership(name::AbstractString, prefixes) -> Union{Nothing, Symbol}\n\nReturn the ownership recorded in the leading type parameter of a carrier type name whose spelling starts with one of prefixes (each ending in {) — :owned for \"CVector{:owned, Float64}\", :borrowed for \"CArray{:borrowed, Float32, 3}\". Return nothing when name matches no prefix or its first parameter is neither token, so that a name carrying no ownership is unrecognized rather than guessed at.\n\n\n\n\n\n","title":"JuliaLibWrapping._carrier_ownership"},{"category":"method","location":"api/#JuliaLibWrapping._classify_return_type-Tuple{Union{Nothing, Int64}, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}, Dict{Int64, String}, Bool}","page":"API reference","text":"_classify_return_type(type_id, typeinfo, typedict, release_present; method=nothing, pass_opaque=false) -> NamedTuple\n\nClassify a return type, identified by its typeinfo id, for façade auto-wrapping. method is the entrypoint whose return type this is, or nothing when there is none, as in the recursive JLWResult call below. release_present is _release_symbols_present's verdict for the surrounding library. pass_opaque is as in _facade_classify_arg: a raw pointer or unrecognized struct return then classifies :passthrough, except for a shape-matched carrier with an unsupported payload (_unsupported_payload_reason), which stays :opaque. The return is one of:\n\n(kind=:passthrough,) — primitive scalar (including Cvoid)\n(kind=:jlwresult_unwrap, inner=<NamedTuple>) — a JLWInterop.JLWResult return, recognized via jlwresult_struct_info. This must be checked before every other struct branch below, including the embedded-JLWStatus :opaque branch, because JLWResult also has a status field. inner is this same classification applied to the wrapped value's type; that kind decides the conversion and the free, rooted at _r.value instead of _result (see _emit_facade_return_body/_emit_value_unwrap). An :opaque payload makes the whole return :opaque.\n(kind=:carray_view, classname=…) — a borrowed CArray return: the storage belongs to the caller, so return _result.as_numpy() hands back a zero-copy view and nothing is freed. Not gated on release_present — there is nothing to release.\n(kind=:carray_unwrap, classname=…) — an owning CArray return: inside a try, copy into a fresh numpy array (np.array(_result.as_numpy(), copy=True)) FIRST — never hand back a view over memory about to be freed — then a finally calls _result.free().\n(kind=:cstring_convert, classname=…) — a borrowed CString return: return _result.as_str(). as_str copies, so the result outlives the buffer; nothing is released. Not gated on release_present.\n(kind=:cstring_unwrap, classname=…) — an owning CString return: inside a try, _out = _result.as_str() (a real copy, independent of the buffer); a finally then calls _result.free(), releasing data via jlw_free.\n(kind=:cstrarray_convert, classname=…) — a borrowed CStrArray return: return _result.as_list(). as_list copies, so the result outlives the buffer; nothing is released. Not gated on release_present.\n(kind=:cstrarray_unwrap, classname=…) — an owning CStrArray return: inside a try, _out = _result.as_list() (a real copy, independent of the buffer); a finally then calls _result.free(), releasing data via jlw_free_strings.\n(kind=:cdict_convert, classname=…) — a borrowed CDict return: return _result.as_dict(), no release.\n(kind=:cdict_unwrap, classname=…) — an owning CDict return: inside a try, _out = _result.as_dict(); a finally then calls _result.free(), releasing keys via jlw_free_strings AND values via jlw_free (two separate allocations).\n(kind=:copt_unwrap, classname=…) — return _result.as_optional(); COpt is by-value, so no free is involved (not gated on release_present)\n(kind=:jlwstatus_discard,) — direct JLWStatus return; discard, return None\n(kind=:opaque, reason=…) — bail out. Also returned in place of :carray_unwrap/:cstring_unwrap/:cstrarray_unwrap/:cdict_unwrap when release_present is false: auto-wrapping an owning return with no release entrypoints available would emit a call to a symbol that does not exist.\n\n\n\n\n\n","title":"JuliaLibWrapping._classify_return_type"},{"category":"method","location":"api/#JuliaLibWrapping._cstring_pointee_info-Tuple{JuliaLibWrapping.StructDesc, String, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}, Dict{Int64, String}}","page":"API reference","text":"_cstring_pointee_info(desc, fieldname, typeinfo, typedict) -> NamedTuple\n\nReturn the Python class name and length width of the CString pointed to by the field named fieldname. The caller must first validate the field.\n\n\n\n\n\n","title":"JuliaLibWrapping._cstring_pointee_info"},{"category":"method","location":"api/#JuliaLibWrapping._emit_cstring_array-Tuple{IO, AbstractString, AbstractString, AbstractString, AbstractString, AbstractString, Integer}","page":"API reference","text":"_emit_cstring_array(f, list_var, arr_var, source_expr, item_var, cstring_classname, element_bits)\n\nEmit the \"encode each string into a raw (non-NUL-terminated) buffer, then build a ctypes array of <cstring_classname> structs, each holding that buffer's length and a pointer into it\" template shared, byte-for-byte, by _write_cstrarray_helpers's from_list (list_var=\"bufs\", arr_var=\"arr\", source_expr=\"items\", item_var=\"s\") and _write_cdict_helpers's from_dict (list_var=\"keys\", arr_var=\"karr\", source_expr=\"d.keys()\", item_var=\"k\") — both build a ctypes argument out of an iterable of Python strs, differing only in what the iterable/result Python variables are named. source_expr is the Python expression iterated to produce raw strings; item_var names the loop variable used in the outer comprehension only — the inner per-buffer comprehension's loop variable is always b, so both call sites emit identical text apart from the substituted names. Embedded NUL bytes in the source strings survive intact: each element's length is the exact byte count, not a NUL-terminator search.\n\nelement_bits controls the per-element length check.\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_cstring_array"},{"category":"method","location":"api/#JuliaLibWrapping._emit_facade_api_autowrapper-Tuple{IO, JuliaLibWrapping.MethodDesc, Any}","page":"API reference","text":"_emit_facade_api_autowrapper(f, method, plan)\n\nEmit an @api-sourced façade wrapper (plan.category === :api_auto): the Python name from plan.api_entry[\"name\"], positional parameters for its \"args\", keyword-only parameters for its \"kwargs\" (with a translated default, or bare when the entry has no \"default\" key), and its \"doc\" as the docstring, escaped by _python_docstring. Argument conversion and the return body match _emit_facade_autowrapper; only the def line's name and parameter shape differ.\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_facade_api_autowrapper"},{"category":"method","location":"api/#JuliaLibWrapping._emit_facade_autowrapper-Tuple{IO, JuliaLibWrapping.MethodDesc, Any}","page":"API reference","text":"_emit_facade_autowrapper(f, method, plan)\n\nEmit an automatically classified façade wrapper.\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_facade_autowrapper"},{"category":"method","location":"api/#JuliaLibWrapping._emit_facade_return_body-Tuple{IO, AbstractString, Any}","page":"API reference","text":"_emit_facade_return_body(f, call, ret)\n\nEmit return handling for the low-level expression call and classification ret.\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_facade_return_body"},{"category":"method","location":"api/#JuliaLibWrapping._emit_freed_guard-Tuple{IO, AbstractString, AbstractString}","page":"API reference","text":"_emit_freed_guard(f, classname, ptr_field)\n\nEmit the two-line guard that opens every accessor on an :owned carrier class. free() nulls ptr_field and zeroes the counts beside it, so without this guard an accessor run afterwards reads a null pointer with a zero count and returns an empty result — an empty numpy array over address 0, an empty list, an empty dict, an empty string — which the caller cannot tell from a genuine answer. The guard turns that into a RuntimeError naming the class.\n\nptr_field must be the field free() nulls first, the same one it guards on. Borrowed classes get no guard: nothing nulls their pointer, so it could only fire on a carrier the caller zeroed by hand, and \"already been freed\" would misname what happened to a class that has no free().\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_freed_guard"},{"category":"method","location":"api/#JuliaLibWrapping._emit_length_guard-Tuple{IO, AbstractString, AbstractString, Integer, AbstractString}","page":"API reference","text":"_emit_length_guard(f, indent, expr, bits, what)\n\nEmit a range check before assigning the Python expression expr to a signed bits-wide field. No check is emitted for 64-bit fields. expr must be side-effect free.\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_length_guard"},{"category":"method","location":"api/#JuliaLibWrapping._emit_owned_free_method-Tuple{IO, AbstractString, Vector{String}, Bool, Vector{String}, Vector{String}}","page":"API reference","text":"_emit_owned_free_method(f, buffers_desc, free_lines, release_present, null_fields, zero_fields)\n\nEmit the .free() method for an :owned carrier class. Ownership is a type parameter, so the struct carries no ownership field; idempotence rides on the carrier's own pointer fields, which free() nulls after releasing them. Borrowed classes get no free() at all.\n\nThe guard has to live in the struct's bytes rather than on the Python object: an owning carrier is usually reached as result.value, and every read of a ctypes struct field builds a fresh wrapper over the same buffer, so an attribute set on one wrapper is gone by the next read and a _freed flag would never trip. Field writes reach the shared buffer and do persist.\n\nWithout release entrypoints the body raises a RuntimeError; the façade never reaches it, since an owning return is demoted to a mechanical re-export in that case (see _facade_classify_return), but a _lowlevel caller gets a clear diagnosis instead of an AttributeError.\n\nThe guard reads a null pointer as \"already freed\", so every owning carrier must allocate a non-null buffer even for an empty payload: JLWInterop's constructors call Libc.malloc(max(n, 1) * sizeof(T)) for exactly that reason. A malloc(0) there would return NULL on some platforms and turn free() into a silent no-op that leaks.\n\nbuffers_desc fills in \"buffer\" or \"buffers\" in the docstring; free_lines are the release-call statements (already indented to 8 spaces) emitted only when release_present. null_fields names the pointer (or dimension-tuple) fields to reset — the first doubles as the guard, and as the field every accessor's _emit_freed_guard tests — and zero_fields the integer counts.\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_owned_free_method"},{"category":"method","location":"api/#JuliaLibWrapping._emit_value_unwrap-Tuple{IO, AbstractString, Symbol}","page":"API reference","text":"_emit_value_unwrap(f, root, kind)\n\nEmit conversion of the low-level value root to Python, releasing owned storage in a finally block.\n\n\n\n\n\n","title":"JuliaLibWrapping._emit_value_unwrap"},{"category":"method","location":"api/#JuliaLibWrapping._facade_arg_conversions-Tuple{IO, Vector{String}, Any}","page":"API reference","text":"_facade_arg_conversions(f, argnames, arg_classes) -> Vector{String}\n\nEmit conversions for non-primitive arguments and return the low-level call arguments.\n\n\n\n\n\n","title":"JuliaLibWrapping._facade_arg_conversions"},{"category":"method","location":"api/#JuliaLibWrapping._facade_classify_arg-Tuple{JuliaLibWrapping.ArgDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}, Dict{Int64, String}}","page":"API reference","text":"_facade_classify_arg(arg, typeinfo, typedict; pass_opaque = false) -> NamedTuple\n\nClassify a method argument for façade auto-wrapping. With pass_opaque, an argument outside the emitter's vocabulary — a raw pointer, or a struct a library registered with JLWInterop.carrier_type — classifies :primitive and crosses the façade as it stands, instead of classifying :opaque. Only an @api entrypoint sets it; see _facade_plan. A struct that matched a carrier recognizer's shape but carries an unsupported payload is excluded from that pass-through and stays :opaque (_unsupported_payload_reason). The return is one of:\n\n(kind=:primitive,) — pass-through\n(kind=:carray, classname=…) — a borrowed CArray; wrap with <class>.from_numpy(name)\n(kind=:cstring, classname=…) — a borrowed CString; wrap with <class>.from_str(name)\n(kind=:cstrarray, classname=…) — a borrowed CStrArray; wrap with <class>.from_list(name)\n(kind=:cdict, classname=…) — a borrowed CDict; wrap with <class>.from_dict(name)\n(kind=:copt, classname=…) — wrap with <class>.from_optional(name)\n(kind=:opaque, reason=…) — bail out; emit mechanical re-export instead.\n\n\n\n\n\n","title":"JuliaLibWrapping._facade_classify_arg"},{"category":"method","location":"api/#JuliaLibWrapping._facade_classify_return-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}, Dict{Int64, String}, Bool}","page":"API reference","text":"_facade_classify_return(method, typeinfo, typedict, release_present; pass_opaque = false) -> NamedTuple\n\n_classify_return_type applied to method's own return type.\n\n\n\n\n\n","title":"JuliaLibWrapping._facade_classify_return"},{"category":"function","location":"api/#JuliaLibWrapping._facade_plan","page":"API reference","text":"_facade_plan(method, typeinfo, typedict, release_present, api_entry) -> NamedTuple\n\nDecide whether an entrypoint should be auto-wrapped on the façade. Returns (category, reason, args, ret, uses_numpy, api_entry). An @api entry is wrapped under its declared interface or rejected if a carrier cannot be handled. Other entrypoints use automatic conversion when useful and fall back to a re-export for opaque signatures.\n\n\n\n\n\n","title":"JuliaLibWrapping._facade_plan"},{"category":"method","location":"api/#JuliaLibWrapping._integer_field_info-Tuple{Any}","page":"API reference","text":"_integer_field_info(desc) -> Union{Nothing, NamedTuple}\n\nReturn (; name, bits) for a signed 32- or 64-bit primitive integer, or nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping._integer_field_info"},{"category":"method","location":"api/#JuliaLibWrapping._is_python_identifier-Tuple{AbstractString}","page":"API reference","text":"_is_python_identifier(s) -> Bool\n\nIs s usable as a Python identifier: ASCII letters, digits and underscores, not starting with a digit, not empty, and not a reserved keyword.\n\n\n\n\n\n","title":"JuliaLibWrapping._is_python_identifier"},{"category":"method","location":"api/#JuliaLibWrapping._match_fields-Union{Tuple{N}, Tuple{JuliaLibWrapping.StructDesc, NTuple{N, String}}} where N","page":"API reference","text":"_match_fields(desc::StructDesc, names::NTuple{N,String}) where {N} -> Union{Nothing,NamedTuple}\n\nReturn the fields named by names, in that order, when desc has exactly those fields. Otherwise return nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping._match_fields"},{"category":"method","location":"api/#JuliaLibWrapping._maybe_dump_api_metadata-NTuple{4, AbstractString}","page":"API reference","text":"_maybe_dump_api_metadata(entry, project, libdir, libname; verbose)\n    -> Union{Nothing, NamedTuple{(:path, :metadata)}}\n\nWhen project's Project.toml lists JLWInterop in [deps], run a subprocess (Base.julia_cmd(), --project=project) that includes entry and calls JLWInterop.write_metadata to write the <libname>.jlw.json API metadata sidecar into libdir; returns the sidecar's path and its parsed exports map. Returns nothing when entry is not a single file, project has no JLWInterop dependency, or the entry file registers no @api functions, in which case the empty sidecar is deleted. Each of those three is an error when the entry's sources call @api — a directory entry is scanned through its src/ tree — and an @info under verbose otherwise.\n\nPaths reach the subprocess through ARGS rather than interpolation into the -e script, which would mangle the backslashes in a Windows path. The subprocess reads no input and runs interpreted (--compile=min -O0): it only resolves the project, includes one file, and writes JSON.\n\nThe subprocess instantiates project before loading JLWInterop. A Manifest it creates is removed afterwards and one it re-resolves is written back, so generating metadata leaves project as it was found; juliac instantiates its own copy in a separate temp dir, so nothing else resolves this one.\n\n\n\n\n\n","title":"JuliaLibWrapping._maybe_dump_api_metadata"},{"category":"method","location":"api/#JuliaLibWrapping._python_carray_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"_python_carray_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\ncarray_struct_info with this emitter's type tables applied: nothing unless the struct matches the CArray shape and its element type has a numpy_dtypes entry; otherwise (; eltype, ndim, ownership, dims_bits, ctype, dtype, dims_ctype).\n\n\n\n\n\n","title":"JuliaLibWrapping._python_carray_info"},{"category":"method","location":"api/#JuliaLibWrapping._python_cdict_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"_python_cdict_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\ncdict_struct_info with this emitter's type tables applied: nothing unless the struct matches the CDict shape and its value type has a scalar_payload_types entry; otherwise (; value_type, ownership, length_bits, ctype).\n\n\n\n\n\n","title":"JuliaLibWrapping._python_cdict_info"},{"category":"method","location":"api/#JuliaLibWrapping._python_copt_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"_python_copt_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\ncopt_struct_info with this emitter's type tables applied: nothing unless the struct matches the COpt shape and its payload type has a scalar_payload_types entry; otherwise (; value_type, ctype).\n\n\n\n\n\n","title":"JuliaLibWrapping._python_copt_info"},{"category":"method","location":"api/#JuliaLibWrapping._python_docstring-Tuple{AbstractString}","page":"API reference","text":"_python_docstring(s) -> String\n\nEscape s for use inside a Python \"\"\"…\"\"\" docstring: every backslash becomes \\\\ and every double quote becomes \\\". Escaping each quote individually keeps a \"\"\" in the text from closing the docstring and keeps a trailing quote from running into the closing delimiter.\n\n\n\n\n\n","title":"JuliaLibWrapping._python_docstring"},{"category":"method","location":"api/#JuliaLibWrapping._python_status_path-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"_python_status_path(method::MethodDesc, typeinfo) -> Union{Nothing, String}\n\njlwstatus_location as a Python attribute path from _result to the returned JLWStatus: nothing when the return carries no status, \"\" when the return type is a JLWStatus, and \".<field>\" (the field name through sanitize_python_argname, which escapes Python keywords) when the status is an embedded field.\n\n\n\n\n\n","title":"JuliaLibWrapping._python_status_path"},{"category":"method","location":"api/#JuliaLibWrapping._release_symbols_present-Tuple{ABIInfo}","page":"API reference","text":"_release_symbols_present(abi_info::ABIInfo) -> Bool\n\nReturn whether the ABI exports both carrier-release entrypoints. Targets use this to decide whether owning returns can be wrapped automatically.\n\n\n\n\n\n","title":"JuliaLibWrapping._release_symbols_present"},{"category":"method","location":"api/#JuliaLibWrapping._unsupported_payload_reason-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"_unsupported_payload_reason(desc::StructDesc, typeinfo) -> Union{Nothing, String}\n\nWhy a struct matching a carrier recognizer's shape still has no Python wrapping: its element, value or payload type lies outside numpy_dtypes/scalar_payload_types. nothing when no recognizer shape-matched at all.\n\nThe _python_*_info wrappers collapse both cases to nothing, so the façade classifiers re-run the raw recognizers through this to tell them apart. The distinction decides whether pass_opaque may wave the struct through: a struct no recognizer matched is a library-registered carrier that crosses the façade unconverted, while a shape-matched one holds Julia-allocated storage behind a pointer the façade can neither build nor release, so it must classify :opaque and ask for a hand-wrap.\n\n\n\n\n\n","title":"JuliaLibWrapping._unsupported_payload_reason"},{"category":"method","location":"api/#JuliaLibWrapping._write_atomically-Tuple{Any, AbstractString, AbstractString}","page":"API reference","text":"_write_atomically(emit, path, scratch)\n\nRun emit(io) against a fresh file in scratch and rename it onto path, removing it instead if emit throws, so a failed emission never leaves a truncated or empty file where a complete one is expected. The temporary name is unique, so two builds writing into one directory cannot share it, and it sits in scratch — the same filesystem as path — so the rename is atomic.\n\n\n\n\n\n","title":"JuliaLibWrapping._write_atomically"},{"category":"method","location":"api/#JuliaLibWrapping._write_carray_helpers-Tuple{IO, Any, AbstractString, Bool}","page":"API reference","text":"_write_carray_helpers(f, cainfo, classname, release_present)\n\nEmit the conversion methods on a recognized CArray ctypes class. The class's ownership decides its API: a :borrowed class gets from_numpy (building a carrier over a numpy buffer the caller keeps alive) and as_numpy, and no free() — it never owns anything to release. An :owned class gets as_numpy and free(), and no from_numpy — Python has no Julia allocation to hand over.\n\n\n\n\n\n","title":"JuliaLibWrapping._write_carray_helpers"},{"category":"method","location":"api/#JuliaLibWrapping._write_cdict_helpers-Tuple{IO, Any, AbstractString, Any, Bool}","page":"API reference","text":"_write_cdict_helpers(f, cdinfo, classname, cstring, release_present)\n\nEmit the conversion methods on a recognized CDict ctypes class. The borrowed class gets from_dict and as_dict; the owned class gets as_dict and free(). cstring describes the key type and length width.\n\n\n\n\n\n","title":"JuliaLibWrapping._write_cdict_helpers"},{"category":"method","location":"api/#JuliaLibWrapping._write_cstrarray_helpers-Tuple{IO, Any, AbstractString, Any, Bool}","page":"API reference","text":"_write_cstrarray_helpers(f, csainfo, classname, cstring, release_present)\n\nEmit the conversion methods on a recognized CStrArray ctypes class. The borrowed class gets from_list and as_list; the owned class gets as_list and free(). cstring describes the element type and length width.\n\n\n\n\n\n","title":"JuliaLibWrapping._write_cstrarray_helpers"},{"category":"method","location":"api/#JuliaLibWrapping._write_cstring_helpers-Tuple{IO, Any, AbstractString, Bool}","page":"API reference","text":"_write_cstring_helpers(f, csinfo, classname, release_present)\n\nEmit conversion methods for a recognized CString ctypes class. Borrowed classes also get constructors; owned classes get free().\n\n\n\n\n\n","title":"JuliaLibWrapping._write_cstring_helpers"},{"category":"method","location":"api/#JuliaLibWrapping.build_library-Tuple{AbstractString, AbstractVector{<:AbstractTarget}}","page":"API reference","text":"build_library(entry, targets;\n              project=dirname(entry), libname, libdir=pwd(),\n              abi_path=joinpath(libdir, libname*\".abi.json\"),\n              trim=:safe, compile_ccallable=true,\n              backend=:auto, verbose=false,\n              bundle=false, bundle_dir=joinpath(libdir, libname*\"-bundle\"),\n              privatize=bundle, cpu_target=nothing)\n\nRun the full juliac → ABI JSON → wrapper pipeline in one call.\n\nentry is the path to the Julia source file (or package directory) that juliac will compile; targets is a vector of AbstractTargets that will each receive a write_wrapper call once the ABI JSON is available.\n\nReturns a NamedTuple (library, abi_path, abi_info, target_outputs, backend, bundle_dir, metadata_path). bundle_dir is the path to the produced bundle tree when bundle = true, and nothing otherwise. metadata_path is the path to the <libname>.jlw.json API metadata sidecar (see the \"API metadata sidecar\" section below), or nothing when the entry project has no JLWInterop dependency or defines no @api functions.\n\nAPI metadata sidecar\n\nWhen project's Project.toml lists JLWInterop in [deps], build_library runs a subprocess, before the juliac step, that includes entry and calls JLWInterop.write_metadata to dump every @api-annotated function's name, positional and keyword arguments, and docstring to <libname>.jlw.json in libdir. Once juliac has run, check_metadata_consistency validates the sidecar against the ABI JSON: an unknown symbol or an argument mismatch is a build error. Targets can use the validated metadata's public names, keyword arguments, and docstrings. The current PythonTarget receives it as write_wrapper's api_metadata keyword. An entry file that defines no @api functions produces no sidecar, and every target emits as it would without one.\n\nExample\n\nusing JuliaLibWrapping, JuliaC\nout = mktempdir()\nresult = build_library(\n    joinpath(@__DIR__, \"src/mylib.jl\"),\n    [CTarget(out, \"mylib\"), PythonTarget(out, \"mylib_py\", \"mylib\")];\n    project = @__DIR__,\n    libname = \"mylib\",\n    libdir  = out,\n)\n\nExtended help\n\nBackend\n\nbuild_library drives JuliaC.jl (a weak dependency); load it with using JuliaC before calling this function. backend = :auto (the default) and backend = :juliac are synonyms; the keyword is retained so additional backends can be added without changing the calling interface.\n\nRelative [sources] paths\n\nbuild_library supports relative [sources] paths and relative paths for developed dependencies in a manifest. Because juliac relocates the project, compilation uses a temporary copy with those paths made absolute. The original project is unchanged. Paths must refer to existing files or directories.\n\nBundling\n\nA juliac-produced .so depends on libjulia, a sysimage, stdlibs, and artifacts — none of which a pip install-ing Python user has on their machine. Pass bundle = true to also produce a self-contained directory tree (the juliac --bundle layout) and copy it into every PythonTarget's package. The Python loader generated for those targets searches the bundle first, so the embedded RUNPATH resolves libjulia from inside the wheel at import time.\n\nbundle = true requires the :juliac backend and that each PythonTarget declare a bundle_subdir (e.g. PythonTarget(out, \"mylib_py\", \"mylib\"; bundle_subdir = \"bundle\")). Targets that are not Python (e.g. CTarget) are unaffected — C consumers manage their own linkage.\n\nprivatize salts the bundled libjulia and libjulia-internal with a distinct SONAME prefix, so the loader cannot satisfy this library's runtime dependency from another loaded copy. It defaults to bundle. See the manual section on multiple wrapped libraries in one process.\n\nPrivatization applies to the bundle, so privatize = true with bundle = false is an error rather than a silent no-op. Pass privatize = false alongside bundle = true to opt out.\n\nCPU target\n\ncpu_target sets the multi-microarchitecture target for the compiled library, using the same syntax as the --cpu-target julia flag or the JULIA_CPU_TARGET environment variable (e.g. \"generic;sandybridge,-xsaveopt,clone_all\"). The default, nothing, defers to JULIA_CPU_TARGET if it is set in the environment, or otherwise to the host CPU.\n\n\n\n\n\n","title":"JuliaLibWrapping.build_library"},{"category":"method","location":"api/#JuliaLibWrapping.carray_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"carray_struct_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\nRecognize CArray, CVector, or CMatrix with explicit ownership, signed 32- or 64-bit dimensions, and primitive data. Return (; eltype, ndim, ownership, dims_type, dims_bits), or nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping.carray_struct_info"},{"category":"method","location":"api/#JuliaLibWrapping.cdict_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"cdict_struct_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\nRecognize CDict with explicit ownership, a signed 32- or 64-bit length, CString keys, and primitive values. Return (; value_type, ownership, length_type, length_bits), or nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping.cdict_struct_info"},{"category":"method","location":"api/#JuliaLibWrapping.check_metadata_consistency-Tuple{ABIInfo, AbstractDict}","page":"API reference","text":"check_metadata_consistency(abi_info::ABIInfo, meta::AbstractDict) -> Nothing\n\nValidate an API metadata sidecar (as returned by read_api_metadata) against the ABI JSON produced for the same build. Throws an ErrorException on:\n\na sidecar entry whose symbol has no matching entrypoint in the ABI;\nan argument-count mismatch: length(args) + length(kwargs) vs. the ABI entrypoint's argument count;\nan argument-name mismatch: [args…; kwarg names…] against the ABI entrypoint's argument names, elementwise. Targets associate the lists positionally, so a difference in either name or order would mislabel arguments.\n\n\n\n\n\n","title":"JuliaLibWrapping.check_metadata_consistency"},{"category":"method","location":"api/#JuliaLibWrapping.copt_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"copt_struct_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\nRecognize COpt with a signed 32- or 64-bit has_value and primitive value. Return (; value_type, has_value_type, has_value_bits), or nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping.copt_struct_info"},{"category":"method","location":"api/#JuliaLibWrapping.cstrarray_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"cstrarray_struct_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\nRecognize CStrArray with explicit ownership, a signed 32- or 64-bit length, and a data pointer to a matching CString. Return (; ownership, length_type, length_bits), or nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping.cstrarray_struct_info"},{"category":"method","location":"api/#JuliaLibWrapping.cstring_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"cstring_struct_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\nRecognize CString with explicit ownership, a signed 32- or 64-bit length, and a data pointer to UInt8. Return (; ownership, length_type, length_bits), or nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping.cstring_struct_info"},{"category":"method","location":"api/#JuliaLibWrapping.is_jlwstatus_struct-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"is_jlwstatus_struct(desc::StructDesc, typeinfo) -> Bool\n\nRecognize JLWStatus by name and field layout.\n\n\n\n\n\n","title":"JuliaLibWrapping.is_jlwstatus_struct"},{"category":"method","location":"api/#JuliaLibWrapping.jlwresult_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"jlwresult_struct_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\nRecognize the generated JLWResult{C} return carrier: a struct named JLWResult… whose fields are a JLWStatus (status) and the payload (value). Return (; value_type_id) on a match — the typeinfo id of the value field's type — otherwise nothing.\n\n\n\n\n\n","title":"JuliaLibWrapping.jlwresult_struct_info"},{"category":"method","location":"api/#JuliaLibWrapping.jlwstatus_location-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"jlwstatus_location(method, typeinfo) -> Union{Nothing, NamedTuple}\n\nLocate a JLWStatus in method's return type: nothing when there is none, (; field = nothing) when the return type is a JLWStatus, and (; field = \"<name>\") (the raw ABI field name) when one is an immediately embedded field.\n\n\n\n\n\n","title":"JuliaLibWrapping.jlwstatus_location"},{"category":"method","location":"api/#JuliaLibWrapping.mangle_python!-Tuple{Dict{Int64, String}, Int64, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"mangle_python!(typedict, type_id, typeinfo) -> String\n\nReturn a Python expression naming the ctypes type for type_id. Struct names go through sanitize_for_c (whose output is also a valid Python identifier) with a _<id> collision suffix matching mangle_c!. Pointer types render inline as ctypes.POINTER(...); Ptr{Cvoid} collapses to ctypes.c_void_p Results are memoized in typedict.\n\n\n\n\n\n","title":"JuliaLibWrapping.mangle_python!"},{"category":"method","location":"api/#JuliaLibWrapping.parse_abi_info-Tuple{AbstractDict}","page":"API reference","text":"abi_info = parse_abi_info(parsed::AbstractDict)\n\nBuild an ABIInfo from a parsed juliac ABI-info JSON document. parsed is the dictionary returned by JSON.parsefile (or JSON.parse) on such a file.\n\nSee read_abi_info for the file-based convenience.\n\n\n\n\n\n","title":"JuliaLibWrapping.parse_abi_info"},{"category":"method","location":"api/#JuliaLibWrapping.raw_primitive_pointer_args-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"raw_primitive_pointer_args(method::MethodDesc, typeinfo) -> Vector{Int}\n\nReturn positional indices into method.args for arguments whose static type is a bare Ptr{T} with T a primitive type other than Cvoid. Pointers inside carrier structs are not examined.\n\nA non-empty result identifies an argument with no length, ownership, or layout metadata. Targets can use it to warn users or decline automatic wrapping.\n\n\n\n\n\n","title":"JuliaLibWrapping.raw_primitive_pointer_args"},{"category":"method","location":"api/#JuliaLibWrapping.read_abi_info-Tuple{AbstractString}","page":"API reference","text":"abi_info = read_abi_info(filename::AbstractString)\nabi_info = read_abi_info(io::IO)\n\nRead and parse a juliac ABI-info JSON file, returning an ABIInfo. The first form is equivalent to parse_abi_info(JSON.parsefile(filename)); the second reads the document from a stream.\n\n\n\n\n\n","title":"JuliaLibWrapping.read_abi_info"},{"category":"method","location":"api/#JuliaLibWrapping.read_api_metadata-Tuple{AbstractString}","page":"API reference","text":"read_api_metadata(path::AbstractString) -> AbstractDict\n\nRead the <lib>.jlw.json API metadata sidecar written by JLWInterop.write_metadata and return its exports map — symbol => {\"name\", \"args\", \"kwargs\", \"doc\"} — as JSON.parsefile returns it, a JSON.Object{String,Any} that behaves as an AbstractDict{String,Any}. Throws an ErrorException when the file's jlw_metadata_version is not 1, the only version this reader understands.\n\n\n\n\n\n","title":"JuliaLibWrapping.read_api_metadata"},{"category":"method","location":"api/#JuliaLibWrapping.sanitize_for_c-Tuple{AbstractString}","page":"API reference","text":"sanitize_for_c(str) -> String\n\nReturn str with all non-alphanumeric (non-underscore) characters replaced by _, leading/trailing underscores stripped, and runs of underscores collapsed. Used to coerce Julia identifiers and type names into valid C tokens. Two distinct inputs may collide; callers that need uniqueness (e.g. mangle_c!) suffix the result with a numeric disambiguator.\n\n\n\n\n\n","title":"JuliaLibWrapping.sanitize_for_c"},{"category":"function","location":"api/#JuliaLibWrapping.sanitize_python_argname","page":"API reference","text":"sanitize_python_argname(name) -> String\nsanitize_python_argname(name, seen::Set{String}) -> String\n\nReturn a Python-identifier form of name: characters illegal in identifiers are stripped via sanitize_for_c, an empty result becomes \"_\", a leading digit (legal in juliac-emitted tuple field names like \"1\", \"2\", …, but illegal in a Python identifier) is prefixed with _, and any reserved Python keyword is suffixed with _.\n\nWhen a seen set is supplied, the returned name is also made unique within that scope (callers should pass one Set{String} per scope — e.g. one per function signature, one per struct's field list). If the candidate already appears in seen, an integer suffix (2, 3, …) is appended until the name is fresh, skipping any value that itself already collides — so the result is safe even when sanitized input happens to look like another argument plus a numeric tail. The chosen name is inserted into seen before returning.\n\n\n\n\n\n","title":"JuliaLibWrapping.sanitize_python_argname"},{"category":"method","location":"api/#JuliaLibWrapping.sort_declarations!-Tuple{OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","page":"API reference","text":"sort_declarations!(typedescs) -> forward_declarations\n\nSort typedescs by type dependency (for example, type A containing type B in a field), so that each descriptor appears after its dependencies. The sort modifies typedescs in place.\n\nReturn the type IDs that require C forward declarations because of recursion.\n\n\n\n\n\n","title":"JuliaLibWrapping.sort_declarations!"},{"category":"function","location":"api/#JuliaLibWrapping.standard_build","page":"API reference","text":"standard_build(dir = pwd(); libname, kwargs...)\n\nRun build_library with defaults for the conventional single-library layout:\n\ndir/\n├── Project.toml          # entry project (runtime deps only)\n├── src/\n│   └── <libname>.jl      # @ccallable entrypoints\n└── out/                  # generated artifacts\n\nEmits both a C header and a Python ctypes package (<libname>_py), bundled for distribution. Equivalent to:\n\nbuild_library(joinpath(dir, \"src\", libname*\".jl\"),\n    [CTarget(joinpath(dir, \"out\"), libname),\n     PythonTarget(joinpath(dir, \"out\"), libname*\"_py\", libname;\n                  bundle_subdir = \"bundle\", version = \"0.0.0\")];\n    project = dir, libname, libdir = joinpath(dir, \"out\"),\n    bundle = true, kwargs...)\n\nThe kwargs out, entry, python_package, project, bundle, and version override the defaults above; anything else is forwarded to build_library (e.g. verbose, trim, privatize). project defaults to dir, but can be pointed at a separate location when the on-disk source layout and the entry Project.toml live in different directories. version sets the version in the generated Python package's pyproject.toml (see PythonTarget). For layouts outside this convention, call build_library directly.\n\n\n\n\n\n","title":"JuliaLibWrapping.standard_build"},{"category":"function","location":"api/#JuliaLibWrapping.write_wrapper","page":"API reference","text":"write_wrapper(target::AbstractTarget, abi_info::ABIInfo)\n\nEmit wrapper source files for abi_info into the location described by target. The methods that ship are dispatched on CTarget (one .h file) and PythonTarget (a Python ctypes package directory). Add a method for a new AbstractTarget subtype to support another output language.\n\n\n\n\n\n","title":"JuliaLibWrapping.write_wrapper"},{"category":"method","location":"api/#JuliaLibWrapping.write_wrapper-Tuple{PythonTarget, ABIInfo}","page":"API reference","text":"write_wrapper(dest::PythonTarget, abi_info::ABIInfo; api_metadata = Dict{String, Any}())\n\nEmit the Python ctypes package described by dest/abi_info. api_metadata is the exports map from an @api metadata sidecar (see read_api_metadata), keyed by C symbol. A symbol present there takes its façade wrapper's shape from the sidecar entry: the Python name, the split into positional args and keyword-only kwargs with translated defaults, and the docstring, escaped by _python_docstring. A symbol absent from api_metadata (the default is an empty Dict) gets the mechanical, ABI-derived shape.\n\n\n\n\n\n","title":"JuliaLibWrapping.write_wrapper"},{"category":"section","location":"concepts/#Concepts","page":"Concepts","text":"This page describes the pipeline, data model, bundling, and generated files.","title":"Concepts"},{"category":"section","location":"concepts/#The-pipeline","page":"Concepts","text":"Wrapper generation has three stages:\n\nsource.jl\n  │   juliac\n  ▼\nlib<name>.so + <name>_abi.json\n  │   read_abi_info → parse_abi_info → sort_declarations!\n  ▼\nABIInfo (descriptors in dependency order)\n  │   write_wrapper(target, abi_info)\n  ▼\n.h / Python package\n\nbuild_library chains the whole sequence; the stages are also callable individually when you need to configure each stage or test the emitters against fixture JSON.","title":"The pipeline"},{"category":"section","location":"concepts/#Two-ways-to-write-a-library","page":"Concepts","text":"A library's entrypoints can be written either by hand or generated. Hand-writing a Base.@ccallable function, as described in the rest of this page and in Error handling across the ABI, gives full control over the C ABI, which is what a signature an outside header fixes needs — @api always returns a JLWResult or a JLWStatus. JLWInterop.@api generates the same kind of wrapper from a declared call signature of an ordinary Julia function: the C symbol, the argument and return conversions, and JLWResult error reporting all come from the declared signature, and the wrapper's public name, keyword arguments, and docstring reach binding targets through a metadata sidecar. The declaration defines nothing itself, so it can sit in a binding layer around a package it does not own. See Annotating a library with @api for the type table and rules. The two styles coexist in one library.","title":"Two ways to write a library"},{"category":"section","location":"concepts/#Driving-the-pipeline","page":"Concepts","text":"build_library runs the full juliac → ABI JSON → wrapper pipeline in one call:\n\nusing JuliaLibWrapping\nusing JuliaC\nout = mktempdir()\nresult = build_library(\n    joinpath(@__DIR__, \"src/mylib.jl\"),\n    [CTarget(out, \"mylib\"), PythonTarget(out, \"mylib_py\", \"mylib\")];\n    project = @__DIR__,\n    libname = \"mylib\",\n    libdir  = out,\n)\n\nbuild_library invokes juliac to produce the shared library and ABI JSON, then applies write_wrapper to each target. The pipeline is driven through JuliaC.jl (a weak dependency): load it with using JuliaC before calling build_library.\n\nbuild_library supports relative [sources] paths, including paths in manifests for developed dependencies. It compiles from a temporary copy with absolute paths because juliac relocates the project before compiling. The original project is unchanged.","title":"Driving the pipeline"},{"category":"section","location":"concepts/#The-ABI-data-model","page":"Concepts","text":"juliac assigns every type an integer type_id. The JSON file contains a flat list of type descriptors keyed by that id, plus a list of exported entrypoint methods.\n\nAfter parsing, an ABIInfo carries an OrderedDict{Int, TypeDesc} of descriptors:\n\nPrimitiveTypeDesc — Int32, Float64, etc.\nStructDesc — a sequence of FieldDescs referencing other type ids.\nPointerDesc — a pointer to another type id.\n\nand a list of MethodDescs whose ArgDescs likewise reference types by id. All cross-references use ids, not names; the descriptors form a graph.\n\nsort_declarations! orders the imported declarations. C requires a type to be defined before use, so the dict must be sorted into dependency order. The implementation builds the type-dependency graph (Graphs.jl), finds strongly-connected components for mutually-recursive types, drops the pointer edges within those SCCs to make the graph acyclic, topologically sorts the result, and returns the forward_declared::BitSet that the C emitter uses to insert forward declarations.","title":"The ABI data model"},{"category":"section","location":"concepts/#Emission-targets","page":"Concepts","text":"AbstractTarget is the extension point for new output formats. Each concrete target is a configuration struct passed to write_wrapper(target, abi_info). Two backends ship today:\n\nCTarget — emits a single .h header. Primitive types map through a fixed ctypes table; non-primitive names go through mangle_c! to produce C-safe identifiers (memoized in a per-target typedict, with numeric suffixing on collision). Pointer types are emitted inline as T* rather than as separate typedefs.\nPythonTarget — emits a Python ctypes package with the two-tier layout described below.\n\nAdding a new target means defining a struct subtype of AbstractTarget and a method write_wrapper(::YourTarget, ::ABIInfo) that walks the sorted descriptors and emits whatever your target language requires. The carrier recognizers in src/recognizers.jl (carray_struct_info, cstring_struct_info, is_jlwstatus_struct, and friends) decide whether a StructDesc/MethodDesc matches a JLWInterop carrier shape by inspecting ABIInfo/TypeDesc alone, so a new target can reuse them as-is instead of reimplementing shape detection.","title":"Emission targets"},{"category":"section","location":"concepts/#Generated-and-editable-Python-modules","page":"Concepts","text":"write_wrapper(PythonTarget, …) emits three files into the generated package directory:\n\n_lowlevel.py — the mechanical ctypes bindings: Structure subclasses and functions carrying the raw C signature. Always regenerated on every write_wrapper call. Each Structure class is followed by a layout check comparing the size and field offsets ctypes computed against the ones the library was compiled with (from the ABI JSON); a divergence raises at import instead of silently misreading fields.\n_facade.py — the package's public API. JuliaLibWrapping creates this file only if it does not exist. Its initial version wraps entrypoints whose arguments and return are recognized — primitive scalars, CVector{owned,T}, CMatrix{owned,T}, CString{owned}, or a direct JLWStatus return — is auto-wrapped to accept and return idiomatic Python objects (numpy arrays, str). Entrypoints with a raw pointer, an unrecognized struct, or an embedded JLWStatus field are re-exported with a # TODO: hand-wrap comment. Delete the file and rerun write_wrapper to regenerate it.\n__init__.py — always regenerated; re-exports names from _facade.\n\nUsers normally import the package-level API from _facade. The generated bindings remain available under pkg._lowlevel when direct access is needed.\n\nRecognition of CArray, CString, and JLWStatus matches the type name, ownership parameter (where present), and field layout. Use JLWInterop for their canonical definitions.","title":"Generated and editable Python modules"},{"category":"section","location":"concepts/#Bundling-for-distribution","page":"Concepts","text":"A juliac library depends on libjulia, a sysimage, stdlibs, and artifacts.\n\nPass bundle = true to build_library to also assemble the full runtime closure (the juliac --bundle layout) and copy it into every PythonTarget's package:\n\nusing JuliaLibWrapping, JuliaC\nout = mktempdir()\nresult = build_library(\n    joinpath(@__DIR__, \"src/mylib.jl\"),\n    [PythonTarget(out, \"mylib_py\", \"mylib\"; bundle_subdir = \"bundle\")];\n    project = @__DIR__,\n    libname = \"mylib\",\n    libdir  = out,\n    bundle  = true,\n)\n\nThe resulting package layout is\n\nout/mylib_py/\n├── __init__.py\n├── _facade.py\n├── _lowlevel.py\n├── pyproject.toml\n└── bundle/\n    ├── lib/\n    │   ├── libmylib.so       # user lib, RUNPATH=$ORIGIN/[/julia]\n    │   ├── libjulia.so.1.13\n    │   └── julia/…           # libjulia-internal, stdlibs, BLAS, …\n    └── artifacts/…\n\nThe loader searches bundle/lib/ before the package root, allowing bundled installs without Julia or LD_LIBRARY_PATH. It falls back to a library beside the package.\n\npip install ./out/mylib_py from a clean virtualenv on a machine without a system Julia is the right manual check.\n\nbundle = true requires the :juliac backend and that each PythonTarget declare a bundle_subdir. The bundle is several hundred megabytes (mostly libLLVM and libjulia-codegen), so it is opt-in. Bundling also salts the bundled libjulia files so they cannot be satisfied by another libjulia already loaded in the process; pass privatize = false to turn that off — see Multiple wrapped libraries in one process.\n\npyproject.toml builds a generic sdist/wheel; distributors must supply any required platform tags or audits.","title":"Bundling for distribution"},{"category":"section","location":"concepts/#Multiple-wrapped-libraries-in-one-process","page":"Concepts","text":"To use multiple wrapped APIs in one process, either compile them into one juliac library or build each bundled library with privatize, the default for bundled builds.\n\njuliac libraries embed libjulia as a runtime dependency. With the default bundle layout each wheel ships its own bundle/lib/libjulia.so.1.13, but the dynamic linker satisfies the second library's DT_NEEDED libjulia.so.1.13 from the first loaded copy. Both libraries then address one runtime, but each expects to initialize it. Imports succeed because initialization is deferred until the first entrypoint call; the first library called works, and the first call into the other aborts the process.\n\nprivatize gives each bundle's libjulia and libjulia-internal a distinct SONAME prefix. The loader then maps each pair independently, and each library initializes its own runtime. The runtimes have separate GC state and resolve runtime symbols to separate addresses.\n\nTwo limits apply to that arrangement:\n\nOnly the Julia runtime is privatized. libopenblas, libblastrampoline, libgfortran, and libunwind keep their usual names and may be shared. Bundles built by the same Julia contain identical copies; bundles built by different Julia versions have not been tested together.\nEach runtime uses its own resources. Two sysimages, two GC heaps, and two thread pools are resident at once.\n\nThis behavior was measured on Linux. macOS and Windows use different library resolution rules and have not been tested.\n\nAt import time, generated _lowlevel.py files record their package names in sys._jlw_loaded_packages. A non-privatized package emits a RuntimeWarning if another wrapped package is already present. Privatized packages do not warn.","title":"Multiple wrapped libraries in one process"},{"category":"section","location":"annotations/#Annotating-a-library-with-@api","page":"Annotating a library with @api","text":"@api declares a C-ABI wrapper for an existing Julia function. It also records the public name, parameters, and docstring for binding targets. The named function must already accept the declared types. Hand-written @ccallable wrappers can coexist with @api; see Two ways to write a library.\n\nWhat is left for a hand-written Base.@ccallable is a signature something outside the library dictates: @api always returns a JLWResult or a JLWStatus, so a function whose exact C signature an existing header fixes has to be written by hand.","title":"Annotating a library with @api"},{"category":"section","location":"annotations/#What-@api-generates","page":"Annotating a library with @api","text":"scale(a::Vector{Float64}; factor::Float64 = 2.0) = factor .* a\n\n@api \"Scale a vector.\" scale(a::Vector{Float64}; factor::Float64 = 2.0)::Vector{Float64}\n\nThe declaration expands to two things:\n\nBase.@ccallable Boundary_scale(a::CArray{:borrowed,Float64,1}, factor::Float64)::JLWResult{CArray{:owned,Float64,1}} — arguments converted with from_carrier, scale called, the result converted with to_carrier and wrapped in jlw_ok, or a caught exception turned into jlw_error.\nAn ApiEntry appended to the declaring module's registry, later written by write_metadata to the <lib>.jlw.json sidecar.\n\nThe macro stores entries in a Vector{ApiEntry} named _JLW_API_REGISTRY_ in the declaring module. This name is reserved. api_entries gathers entries from a module and its submodules.\n\nThe declared types define the ABI contract; the Julia function may accept a broader signature.","title":"What @api generates"},{"category":"section","location":"annotations/#Writing-the-signature","page":"Annotating a library with @api","text":"@api takes the signature apart at expansion time, so it accepts one shape:\n\n@api [docstring] name(a::T1, …; k::K = default, …)::Ret\n\nBodies and where clauses are not supported. Define the function separately.\n\nThe docstring is the macro's first argument, on the same line as @api. A declaration without one falls back to the function's own Julia docstring, so documenting the function the ordinary way is usually enough; give the macro an argument when foreign callers need something different from what a Julia caller reads.\n\nThe function must be defined or imported before the declaration. @api checks that it accepts the declared arguments during macro expansion.\n\nThe public name is the Julia function name. Targets may impose additional identifier rules; for example, the Python target rejects a trailing ! rather than emitting def bump!(…).","title":"Writing the signature"},{"category":"section","location":"annotations/#Type-table","page":"Annotating a library with @api","text":"Every argument must resolve, via carrier_type, and the return type via carrier_return_type, to a C-ABI carrier. An unmapped type is an expansion-time error naming the offending argument or the return. \"Scalar\" below means one of Int8–Int64, UInt8–UInt64, Float32, Float64, Bool; the carriers hold those elements as raw bytes behind a pointer, so Vector{Any}, Matrix{String}, Dict{String,Vector{Int}} and a union of scalars such as Vector{Union{Int64,Float64}} are rejected rather than reinterpreted.\n\nJulia T as argument as return\nInt8–Int64, UInt8–UInt64, Float32, Float64, Bool itself, by value itself, by value\nString CString{:borrowed}, the caller's bytes CString{:owned}, a copy the caller frees\nVector{String} CStrArray{:borrowed}, copied (Julia-side mutation is invisible to the caller) CStrArray{:owned}\nDict{String,V} (V scalar) CDict{:borrowed,V}, copied CDict{:owned,V}\nUnion{T,Nothing} (T scalar) COpt, by value COpt, by value\nArray{T,N} / Vector{T} / Matrix{T} (T scalar) CArray{:borrowed,T,N}, a zero-copy view (unsafe_wrap, own=false) — mutation through the argument is visible to the caller CArray{:owned,T,N}, a fresh allocation\nNothing (return only) — bare JLWStatus, no value field\nPtr{T} itself, by value itself, by value\na type the library registers (see Registering your own type) its own carrier its own carrier\n\nEach carrier states its ownership in its type, so an argument and a return of the same Julia type are two distinct carrier types and the release obligation is visible in the wrapper's signature; see Owning carrier returns in JLWInterop for what a target does with each.\n\nThe Array argument's zero-copy view is valid only for the duration of the call: a function that stores it for later use reads memory the caller may have freed or repurposed by then. And because mutation is visible to the caller, a caller must not pass read-only storage to a function that mutates its argument. For example, this excludes a NumPy array with writeable=False.\n\nA Ptr{T} carries only an address. The caller must keep its memory alive for the call. Use Vector{T} when the length must cross the boundary.\n\nReturning a Ptr{T} is allowed and puts the contract entirely in your hands. The address must still be one the caller can reach after the call returns — a pointer into a buffer it passed in, or into storage the library keeps. A pointer into a fresh Julia allocation dangles once the collector runs, and one into Libc.malloc memory leaks unless the target provides a matching release contract. Return an Array when the caller should own the buffer: it gets a length and a release path.\n\nThe examples/boundary example exercises every row except the multi-dimensional Array{T,N} form; its arrays are all Vectors. Its declarations live in a binding layer beside the package — see Where the declarations live.","title":"Type table"},{"category":"section","location":"annotations/#Registering-your-own-type","page":"Annotating a library with @api","text":"The mapping is a set of ordinary methods, so a library can extend it for a type of its own. An isbits struct can be its own carrier: it already has a C layout, and the three methods are the identity.\n\nstruct Extent\n    lo::Int32\n    hi::Int32\nend\n\nJLWInterop.carrier_type(::Type{Extent}) = Extent\nJLWInterop.to_carrier(e::Extent) = e\nJLWInterop.from_carrier(::Type{Extent}, c::Extent) = c\n\nfunction widen(e::Extent, by::Int32)\n    by >= 0 || error(\"negative width\")\n    return Extent(e.lo - by, e.hi + by)\nend\n\n@api \"Widen an extent by `by` on both sides.\" widen(e::Extent, by::Int32)::Extent\n\nDefine these methods before the @api declaration. The carrier must be isbits because the error branch constructs a zero-filled value. The wrapper is Base.@ccallable Boundary_widen(e::Extent, by::Int32)::JLWResult{Extent}, and targets can expose its fields directly. For example, the Python target generates a ctypes.Structure:\n\nwide = widen(Extent(1, 5), 2)   # wide.lo == -1, wide.hi == 7\n\nTargets may pass a Ptr or registered struct through without conversion. The current Python façade does so for ctypes values while still applying the declared name, keyword arguments, docstring, and JLWResult error handling.","title":"Registering your own type"},{"category":"section","location":"annotations/#Keyword-arguments","page":"Annotating a library with @api","text":"A trailing ; k::K = default, … block is appended to the positional C arguments in declaration order. The metadata retains the positional/keyword split and defaults so each target can express them in its own calling conventions. For example, the Python target emits keyword-only parameters; a keyword without a default is required.\n\nDefaults must be literals: Int, Float, Bool, String, or nothing (a negated numeric literal such as -1 or -1.5 is accepted too). Anything else — a symbol, an expression, [] — fails at expansion with keyword '<k>' default must be a literal. The literal must also have the keyword's declared type, so k::Float64 = 2 is rejected and k::Float64 = 2.0 is accepted.\n\nThe default travels to the sidecar as a JSON value of its own type — a number, a JSON string, true/false, or null — and the Python target writes that value as a Python literal, None for null.\n\nsum_dict(d::Dict{String,Float64}; scale::Float64 = 1.0) =\n    scale * sum(values(d); init = 0.0)\n\n@api sum_dict(d::Dict{String,Float64}; scale::Float64 = 1.0)::Float64\n\nbecomes def sum_dict(d, *, scale=1.0) in the generated façade.","title":"Keyword arguments"},{"category":"section","location":"annotations/#Errors","page":"Annotating a library with @api","text":"Every @api wrapper wraps its call in try/catch. A caught exception becomes a JLWResult whose status.code names the exception type:\n\ncode exception message\n1 ErrorException, and anything not listed below e.msg, or the exception's type name\n2 ArgumentError e.msg\n3 DimensionMismatch e.msg\n4 InexactError \"InexactError\"\n5 BoundsError \"BoundsError\"\n\nA recognized String or SubString{String} message is preserved. Other message types and unrecognized exceptions report the exception type name.\n\nThe declared return type is applied to the returned value inside the try, so a function whose actual return type differs from the declaration reports that as an ordinary error rather than failing in the wrapper's own return conversion, which the catch cannot reach.\n\nTargets decide how to report a non-zero status.code. The Python target raises JLWError, a RuntimeError subclass carrying .code and .message. See Error handling across the ABI for the underlying JLWStatus convention.\n\nboom(x::Int64) = error(\"boom $x\")\n\n@api \"Always throws.\" boom(x::Int64)::Int64\n\ntry:\n    boom(3)\nexcept JLWError as e:\n    print(e.code, e.message)   # 1, \"boom 3\"","title":"Errors"},{"category":"section","location":"annotations/#Symbol-naming","page":"Annotating a library with @api","text":"The generated C symbol is join(fullname(mod), \"_\") * \"_\" * name, with a leading Main component stripped. A.B.f and C.B.f therefore produce distinct symbols (A_B_f, C_B_f) even though nameof alone would collide. The sidecar also stores the unqualified public name, allowing a target to expose it instead of the C symbol.","title":"Symbol naming"},{"category":"section","location":"annotations/#Where-the-declarations-live","page":"Annotating a library with @api","text":"A declaration defines nothing, so it does not have to sit in the package it exposes. Two layouts work.\n\nA binding layer beside the package. The package stays as it is, and a second project holds the declarations:\n\nFoo/\n├── Project.toml          # what a Julia user installs — no JLWInterop dep\n├── src/Foo.jl\n└── lib/\n    ├── Project.toml      # [sources] Foo = {path = \"..\"}\n    └── src/foo.jl        # `using Foo: scale` + the @api declarations\n\nbuild_library is pointed at lib/. The package gains no dependency and no entrypoints, and someone who does not maintain it can still write its bindings. Importing the wrapped functions by name is safe here precisely because a declaration cannot define a method on them. Relative [sources] paths are resolved by compiling a temporary copy with them made absolute, so lib/Project.toml stays committable. examples/boundary is built this way.\n\nDeclarations inside the package. Simpler, at the cost of a JLWInterop dependency for every Julia user of the package and entry points compiled into it.","title":"Where the declarations live"},{"category":"section","location":"annotations/#The-include-tree-limitation","page":"Annotating a library with @api","text":"build_library dumps the metadata sidecar by spawning a subprocess that includes the entry file and calls write_metadata. @api must therefore live in the entry file's include tree — a file included, directly or transitively, from the file passed to build_library. An @api inside a package loaded with using/import does not register: its precompilation ran in a different process, and the dump subprocess never re-executes that package's top-level code, so no ApiEntry is pushed in the process that writes the sidecar.\n\nThis constrains the declarations, not the functions they name. In the binding-layer form the declarations are in the entry file and the package is an ordinary using dependency, which is what the rule asks for.","title":"The include-tree limitation"},{"category":"section","location":"annotations/#Owning-returns-require-@export_release_entrypoints","page":"Annotating a library with @api","text":"Owning Vector{String}, Dict, and array returns need the release entrypoints described in Owning carrier returns in JLWInterop. @api does not emit them itself — a macro cannot know which of its expansions is the last one in a library — so add one @export_release_entrypoints at the library's top level:\n\nmodule Boundary\nusing JLWInterop\n\n@export_release_entrypoints\n\nupcase_strs(a::Vector{String}) = uppercase.(a)\n@api upcase_strs(a::Vector{String})::Vector{String}\nend\n\nOmitting it fails the build:\n\ncannot wrap `@api` entrypoint 'Boundary_upcase_strs': owning return needs release entrypoints; add JLWInterop.@export_release_entrypoints to the library\n\nA target cannot automate ownership without the required release symbols. The current Python target treats their absence as a build error for @api entries rather than discarding the declared interface.\n\nA hand-written @ccallable never appears in the sidecar, so the Python target instead re-exports it with a TODO comment naming the macro:\n\nfrom ._lowlevel import Boundary_upcase_strs  # TODO: hand-wrap — owning return needs release entrypoints; add JLWInterop.@export_release_entrypoints to the library","title":"Owning returns require @export_release_entrypoints"},{"category":"section","location":"annotations/#Python-target-example","page":"Annotating a library with @api","text":"result = build_library(\n    joinpath(@__DIR__, \"lib/src/boundary.jl\"),\n    [PythonTarget(out, \"boundary_py\", \"boundary\")];\n    project = joinpath(@__DIR__, \"lib\"), libname = \"boundary\", libdir = out,\n)\n\nruns, in order: a subprocess that includes entry.jl and writes boundary.jlw.json; juliac, which writes boundary.abi.json; then the Python target, which merges both by C symbol and writes _facade.py. A symbol present in the ABI JSON with no sidecar entry falls back to the mechanical, unnamed wrapping every entrypoint gets without @api.\n\nBoth of them execute the entry file's top level, in two separate processes, so that top level must be safe to run twice: it may define modules, functions and constants, but not do work whose repetition would be wrong, such as writing to a fixed output path or appending to a file.\n\n_facade.py is written once and never regenerated — write_wrapper creates it only when the file is absent, so an author's edits survive a rebuild. After changing an @api signature, delete _facade.py and rerun; otherwise the old wrapper keeps calling the regenerated _lowlevel with the old argument list.","title":"Python target example"},{"category":"section","location":"annotations/#Call-path","page":"Annotating a library with @api","text":"scale(a::Vector{Float64}; factor::Float64 = 2.0) = factor .* a\n@api scale(a::Vector{Float64}; factor::Float64 = 2.0)::Vector{Float64}\n\nscale(a, factor=3.0)\n\ncalls the generated Boundary_scale, which converts arguments, calls scale, and returns a JLWResult. The façade checks status.code: zero converts value to the idiomatic Python type, freeing an owning carrier in a finally; non-zero raises JLWError(status.code, status.message).","title":"Call path"},{"category":"section","location":"annotations/#Internals","page":"Annotating a library with @api","text":"Private helpers referenced by the docstrings above:","title":"Internals"},{"category":"macro","location":"annotations/#JLWInterop.@api","page":"Annotating a library with @api","text":"@api [docstring] name(a::T1, ...; k::K = default, ...)::Ret\n\nDeclare one call signature of name as a JuliaLibWrapping API entry point. name must already be callable with those types — defined in this module, or brought in from the package this binding layer wraps. The macro defines nothing itself: it generates a Base.@ccallable C-ABI wrapper (named per _api_symbol) that converts arguments and return value through carrier_type/carrier_return_type/to_carrier/from_carrier and reports errors via JLWResult/JLWStatus, and records an ApiEntry in the declaring module's registry (see _REGISTRY_NAME).\n\nThe declared types are the boundary contract, not a method signature: name may accept more than this, and foreign callers get exactly this. Declaring a signature name cannot satisfy is caught when the library is compiled, as a missing method.\n\nA body is rejected. Writing one would define a function, and in a binding layer that imports the wrapped function by name it would silently add a method to it instead — which recurses when the declared signature is the more specific one.\n\nEvery argument and the return type must resolve (via Core.eval in the declaring module) to a type with a carrier mapping. Types are resolved at macro-expansion time, so any alias used in the signature must already be defined. Keyword defaults must be literals (Int, Float, Bool, String, or nothing) of the keyword's declared type: k::Float64 = 2 is rejected, k::Float64 = 2.0 is accepted.\n\nKeyword arguments are positional in the C ABI. They follow the positional arguments, in declaration order, and every one is passed on every call: a default is applied by the calling side, which reads it from the metadata sidecar, not by the wrapper. The wrapper therefore has one arity, and a keyword's default is a property of the binding rather than of the entry point.\n\n\n\n\n\n","title":"JLWInterop.@api"},{"category":"type","location":"annotations/#JLWInterop.ApiEntry","page":"Annotating a library with @api","text":"ApiEntry\n\nMetadata for an @api declaration: its Julia name, C symbol, positional args, keyword kwargs, return type ret, and docstring.\n\nA keyword's default is the literal value itself, already of type type; has_default is false for a required keyword, and its default is then nothing.\n\n\n\n\n\n","title":"JLWInterop.ApiEntry"},{"category":"function","location":"annotations/#JLWInterop.api_entries","page":"Annotating a library with @api","text":"api_entries(root::Module = Main) -> Vector{ApiEntry}\n\nEvery @api entry declared in root or in a module nested under it, in declaration order per module. Base and Core are not searched.\n\n\n\n\n\n","title":"JLWInterop.api_entries"},{"category":"function","location":"annotations/#JLWInterop.carrier_type","page":"Annotating a library with @api","text":"carrier_type(::Type{T}) -> Union{Type, Nothing}\n\nThe C-ABI argument carrier for T, or nothing when no mapping exists. Storage-backed argument carriers are borrowed. Dictionary values, optional payloads, and array elements must be concrete scalar bits types.\n\nA Ptr{T} is its own carrier and crosses unconverted: neither a length nor an owner travels with it. An argument addresses memory the caller owns. A return must address memory that outlives the call and that the caller can already reach — a buffer it passed in, or storage the library holds onto. A pointer into a fresh Julia allocation dangles as soon as the collector runs; one into Libc.malloc memory leaks, because nothing on the far side knows to free it.\n\nThe mapping is open. A type outside this table becomes an @api type once its own module adds methods for it to carrier_type, to_carrier and from_carrier; an isbits struct can be its own carrier, with all three methods the identity.\n\n\n\n\n\n","title":"JLWInterop.carrier_type"},{"category":"function","location":"annotations/#JLWInterop.carrier_return_type","page":"Annotating a library with @api","text":"carrier_return_type(::Type{T}) -> Union{Type, Nothing}\n\nThe C-ABI return carrier for T. Storage-backed return carriers are owned; other mappings match carrier_type.\n\n\n\n\n\n","title":"JLWInterop.carrier_return_type"},{"category":"function","location":"annotations/#JLWInterop.to_carrier","page":"Annotating a library with @api","text":"to_carrier(x) -> carrier value\n\nConvert a native value to its C-ABI carrier, per carrier_type.\n\n\n\n\n\n","title":"JLWInterop.to_carrier"},{"category":"function","location":"annotations/#JLWInterop.to_carrier_as","page":"Annotating a library with @api","text":"to_carrier_as(::Type{T}, x) -> carrier\n\nConvert x to T, then to its carrier.\n\n\n\n\n\n","title":"JLWInterop.to_carrier_as"},{"category":"function","location":"annotations/#JLWInterop.from_carrier","page":"Annotating a library with @api","text":"from_carrier(::Type{T}, c) -> T\n\nConvert a C-ABI carrier back to the native T.\n\n\n\n\n\n","title":"JLWInterop.from_carrier"},{"category":"function","location":"annotations/#JLWInterop.write_metadata","page":"Annotating a library with @api","text":"write_metadata(path::AbstractString, root::Module = Main)\n\nWrite the JSON metadata sidecar for every @api declaration in root or a module nested under it (see api_entries) to path:\n\n{\"jlw_metadata_version\": 1, \"exports\": {symbol: {\"name\", \"args\", \"kwargs\", \"doc\"}}}\n\nexports entries are sorted by symbol for stable output. args is the positional argument names, in declaration order. kwargs is a list of {\"name\": ...} for a required keyword-only argument (see ApiEntry) or {\"name\": ..., \"default\": ...} for one with a default, written as a JSON value of its own type: a number, a JSON string, true/false, or null. Types are not repeated here: they live in the separate ABI JSON that juliac produces, keyed by the same symbol.\n\nThe JSON is written by hand so that JLWInterop needs no JSON dependency.\n\n\n\n\n\n","title":"JLWInterop.write_metadata"},{"category":"type","location":"annotations/#JLWInterop.JLWResult","page":"Annotating a library with @api","text":"JLWResult{C}\n\nReturn carrier for a generated API wrapper: a JLWStatus plus a value. A zero status.code means value is meaningful; on any nonzero code value is zero-filled, and its pointers are null.\n\n\n\n\n\n","title":"JLWInterop.JLWResult"},{"category":"function","location":"annotations/#JLWInterop.to_carrier_opt","page":"Annotating a library with @api","text":"to_carrier_opt(::Type{T}, x) -> COpt{T}\n\nConvert a native Union{T,Nothing} value to its COpt{T} carrier. to_carrier cannot dispatch on such a value, since a bare nothing carries no information about T, so @api calls this for optional returns with T fixed at macro-expansion time.\n\n\n\n\n\n","title":"JLWInterop.to_carrier_opt"},{"category":"function","location":"annotations/#JLWInterop._api_symbol","page":"Annotating a library with @api","text":"_api_symbol(mod::Module, name::Symbol) -> String\n\nThe C symbol for name defined in mod: join(fullname(mod), \"_\") * \"_\" * name, with a leading Main component stripped.\n\n\n\n\n\n","title":"JLWInterop._api_symbol"},{"category":"constant","location":"annotations/#JLWInterop._REGISTRY_NAME","page":"Annotating a library with @api","text":"JLWInterop._JLW_API_REGISTRY_\n\nReserved name for each module's Vector{ApiEntry} registry.\n\n\n\n\n\n","title":"JLWInterop._REGISTRY_NAME"},{"category":"function","location":"annotations/#JLWInterop._register!","page":"Annotating a library with @api","text":"_register!(registry::Vector{ApiEntry}, e::ApiEntry)\n\nAppend e, rejecting duplicate symbols.\n\n\n\n\n\n","title":"JLWInterop._register!"},{"category":"constant","location":"annotations/#JLWInterop._API_ERROR_CODES","page":"Annotating a library with @api","text":"JLWInterop._API_ERROR_CODES\n\nThe JLWStatus code an @api wrapper reports per exception type:\n\ncode exception\n1 ErrorException, and anything not listed below\n2 ArgumentError\n3 DimensionMismatch\n4 InexactError\n5 BoundsError\n\nA target can branch on the code and still show the message. Codes are part of the ABI: renumbering one is a breaking change.\n\n\n\n\n\n","title":"JLWInterop._API_ERROR_CODES"},{"category":"constant","location":"annotations/#JLWInterop._API_ERROR_TABLE","page":"Annotating a library with @api","text":"JLWInterop._API_ERROR_TABLE\n\nThe exception types an @api wrapper recognizes, as (type, code, carries a msg field). Recognizing another one is a row here; _api_status_expr builds the wrapper's isa chain from it in this order, so a subtype must precede its supertype.\n\n\n\n\n\n","title":"JLWInterop._API_ERROR_TABLE"},{"category":"function","location":"annotations/#JLWInterop._api_status_expr","page":"Annotating a library with @api","text":"_api_status_expr(e::Symbol) -> Expr\n\nAn expression evaluating to (code, message) for the exception bound to e.\n\nThe generated isa chain avoids dynamic dispatch in trimmed libraries. e names the caught exception; all temporary bindings are generated symbols.\n\n\n\n\n\n","title":"JLWInterop._api_status_expr"},{"category":"function","location":"annotations/#JLWInterop._api_as","page":"Annotating a library with @api","text":"_api_as(::Type{T}, x) -> T\n\nConvert x to the declared return type inside the wrapper's error boundary.\n\n\n\n\n\n","title":"JLWInterop._api_as"},{"category":"function","location":"annotations/#JLWInterop._julia_docstring","page":"Annotating a library with @api","text":"_julia_docstring(mod::Module, name::Symbol, sig::Type) -> String\n\nThe docstring attached to mod.name for sig, or the first one attached to it under any signature, or \"\" when it has none. A docstring written on the function rather than on a method is filed under no signature, which is why the lookup falls back.\n\nUses Base.Docs.meta for compatibility across supported Julia versions.\n\n\n\n\n\n","title":"JLWInterop._julia_docstring"},{"category":"function","location":"annotations/#JLWInterop._zero_carrier","page":"Annotating a library with @api","text":"JLWInterop._zero_carrier(::Type{C}) -> C\n\nBuild a zero-filled C for the failure branch of jlw_error: every pointer is null and every count is zero, so releasing it is a no-op. Each built-in carrier has its own method; any other isbits carrier — a raw Ptr, or a struct a library registers with carrier_type — is zeroed byte-wise, covering whatever padding it has. A carrier that is not isbits throws an ArgumentError.\n\n\n\n\n\n","title":"JLWInterop._zero_carrier"},{"category":"function","location":"annotations/#JLWInterop._api_opt_inner","page":"Annotating a library with @api","text":"_api_opt_inner(T::Type) -> Union{Type, Nothing}\n\nReturn the payload type of Union{T,Nothing}, or nothing for other types.\n\n\n\n\n\n","title":"JLWInterop._api_opt_inner"},{"category":"section","location":"jlwinterop/#JLWInterop","page":"JLWInterop","text":"","title":"JLWInterop"},{"category":"section","location":"jlwinterop/#Ownership-and-layout","page":"JLWInterop","text":"The package defines fixed-layout types for passing values across a C ABI. Borrowed values do not own their underlying storage; the caller must keep the storage alive for the duration of the call. CArray, CString, CStrArray, and CDict each state their ownership in a leading :owned/:borrowed type parameter. The types are isbits when their element types are, work with juliac --trim, and cross a @ccallable boundary without allocating a Julia object.\n\nJuliaLibWrapping targets recognize these types structurally, by name and field layout. Using JLWInterop keeps those layouts consistent across libraries.","title":"Ownership and layout"},{"category":"section","location":"jlwinterop/#JLWStatus-—-in-band-error-reporting","page":"JLWInterop","text":"A library that needs to report errors to its caller (rather than abort the process) returns either a JLWStatus directly, or a struct that contains a JLWStatus field. code == 0 is success; any non-zero value is an error code the library defines. message is a fixed-size UTF-8 buffer, null-terminated within the buffer.\n\nThe buffer is inline and fixed-size (JLW_MESSAGE_BYTES bytes) on purpose: a Cstring or Ptr{UInt8} would force a decision about who allocates and frees the message, which has no good answer under juliac --trim. The price is a bounded message length; the benefit is that constructing a status performs no heap allocation.\n\nConstruct values with the helpers:\n\nusing JLWInterop\n\nBase.@ccallable function safe_sqrt(x::Float64)::JLWStatus\n    x < 0 && return jlw_error(1, \"negative input\")\n    return jlw_ok()\nend\n\nSee Error handling across the ABI for the protocol and its current Python mapping.","title":"JLWStatus — in-band error reporting"},{"category":"section","location":"jlwinterop/#CArray{owned,T,N}-—-N-D-numeric-buffer-(column-major)","page":"JLWInterop","text":"CArray{owned,T,N} is (dims::NTuple{N,Int64}, data::Ptr{T}) in column-major order. Targets may map this layout to native array types.","title":"CArray{owned,T,N} — N-D numeric buffer (column-major)"},{"category":"section","location":"jlwinterop/#CArray-ownership-contract","page":"JLWInterop","text":"Ownership is the leading type parameter, :owned or :borrowed, so the two flavors are two distinct types with identical layout.\n\nCArray{:borrowed,T,N} wraps memory the caller owns. The caller keeps it alive and makes it writable before mutation; the consumer never releases it. CArray{:borrowed}(A::DenseArray) aliases A's own storage (pointer(A)) without copying; other array types are refused, since only DenseArray guarantees the contiguous column-major layout the carrier promises. Pointer constructors build carriers of either ownership for callers who vouch for the layout themselves.\nCArray{:owned,T,N} holds a Julia allocation. CArray{:owned}(A) Libc.mallocs a dense column-major copy of A; the consumer releases data exactly once.\n\nThere is no ownership-defaulting constructor, and any parameter other than :owned or :borrowed is rejected, so an ownership is never guessed.\n\nThe one- and two-dimensional aliases are:\n\nconst CVector{owned,T} = CArray{owned,T,1}\nconst CMatrix{owned,T} = CArray{owned,T,2}\n\nCArray{owned,T,N} <: AbstractArray{T,N} with linear indexing. It supports standard array operations; mutate only writable storage.","title":"CArray ownership contract"},{"category":"section","location":"jlwinterop/#Python-target","page":"JLWInterop","text":"A borrowed return becomes a zero-copy numpy view: the façade calls as_numpy() and hands the view back, because the storage stays the caller's. An owning return is copied into a fresh numpy array and the Julia allocation is released in a finally. The generated classes follow: a borrowed class gets from_numpy and as_numpy and no free(); an owning class gets as_numpy and an idempotent free(), and no from_numpy.\n\nA hand-written entrypoint returning an owning CArray without @export_release_entrypoints is demoted at build time to a TODO re-export naming the macro to add — the same rule as CString, CStrArray, and CDict. An @api function in that position fails the build instead. An owning CArray argument is likewise left to a human: it would transfer a Julia allocation into the library, which numpy cannot supply. The same holds for owning CString, CStrArray, and CDict arguments.\n\nFor N ≥ 2, the generated from_numpy helper requires a Fortran-contiguous array. Convert row-major input with np.asfortranarray.","title":"Python target"},{"category":"section","location":"jlwinterop/#C-target","page":"JLWInterop","text":"The two ownerships mangle to two distinct typedefs (CVector_owned_Float64, CVector_borrowed_Float64), so whether to free a returned buffer is visible in the signature.","title":"C target"},{"category":"section","location":"jlwinterop/#CString{owned}-—-length-prefixed-UTF-8","page":"JLWInterop","text":"CString{owned} is (length::Int64, data::Ptr{UInt8}). It is length-prefixed, so it permits embedded NUL bytes.\n\nCString{owned} <: AbstractString; call String(s) to copy the bytes into a Julia String.","title":"CString{owned} — length-prefixed UTF-8"},{"category":"section","location":"jlwinterop/#CString-ownership-contract","page":"JLWInterop","text":"owned is :owned or :borrowed; both types have the same layout.\n\nCString{:borrowed} wraps a buffer the caller owns and keeps alive; the consumer never releases it.\nCString{:owned}(::AbstractString) allocates a copy of the UTF-8 bytes. The consumer releases data once with jlw_free.","title":"CString ownership contract"},{"category":"section","location":"jlwinterop/#Python-target-2","page":"JLWInterop","text":"Julia Python\nAbstractString str\n\nA borrowed class gets from_str, from_bytes, as_str, and as_bytes. An owning class gets the conversion methods and an idempotent free(), but no constructors. Owning returns are decoded and freed in a finally; owning arguments require a manual wrapper.","title":"Python target"},{"category":"section","location":"jlwinterop/#CStrArray{owned}-—-string-arrays","page":"JLWInterop","text":"CStrArray{owned} is (length::Int64, data::Ptr{CString{owned}}): a pointer to length CStrings with matching ownership. Converting it to Vector{String} copies the strings without freeing the source.","title":"CStrArray{owned} — string arrays"},{"category":"section","location":"jlwinterop/#CStrArray-ownership-contract","page":"JLWInterop","text":"owned is :owned or :borrowed, and the two are distinct types with the same layout.\n\nCStrArray{:borrowed} wraps storage the caller owns and keeps alive; the consumer never releases it.\nCStrArray{:owned} holds a Julia allocation, produced by CStrArray{:owned}(::Vector{String}). The consumer releases it once with jlw_free_strings. There is no borrowing constructor: a Julia Vector{String} has no length-prefixed layout to alias.","title":"CStrArray ownership contract"},{"category":"section","location":"jlwinterop/#Python-target-3","page":"JLWInterop","text":"Julia Python\nVector{String} list[str]\n\nA borrowed class gets from_list and as_list; an owning class gets as_list and an idempotent free(). Owning returns are copied and freed in a finally.","title":"Python target"},{"category":"section","location":"jlwinterop/#CDict{owned,V}-—-string-keyed-dictionaries","page":"JLWInterop","text":"CDict{owned,V} is (length::Int64, keys::Ptr{CString{owned}}, values::Ptr{V}): two parallel arrays. Keys are CStrings with matching ownership; values use a type in CDICT_VALUE_TYPES. Converting to a Julia Dict copies without freeing the source.","title":"CDict{owned,V} — string-keyed dictionaries"},{"category":"section","location":"jlwinterop/#CDict-ownership-contract","page":"JLWInterop","text":"owned is :owned or :borrowed, and the two are distinct types with the same layout.\n\nCDict{:borrowed,V} wraps storage the caller owns and keeps alive; the consumer never releases it.\nCDict{:owned,V} holds two separate Julia allocations, produced by CDict{:owned}(::Dict). keys is released with jlw_free_strings and values with jlw_free, each exactly once. There is no borrowing constructor: a Dict's storage is neither length-prefixed strings nor a dense value array.","title":"CDict ownership contract"},{"category":"section","location":"jlwinterop/#Python-target-4","page":"JLWInterop","text":"Julia Python\nDict{String,V} dict[str, V]\n\nA borrowed class gets from_dict and as_dict; an owning class gets as_dict and an idempotent free(). Owning returns are copied and freed in a finally.","title":"Python target"},{"category":"section","location":"jlwinterop/#COpt{T}-—-optional-scalars","page":"JLWInterop","text":"COpt represents Union{T,Nothing} with an Int32 discriminant and an inline value. It is allocation-free and has no ownership state.","title":"COpt{T} — optional scalars"},{"category":"section","location":"jlwinterop/#Python-target-5","page":"JLWInterop","text":"Julia Python\nUnion{T,Nothing} for scalar T T | None\n\nThe generated from_optional and as_optional helpers convert the value without a keepalive or release operation.","title":"Python target"},{"category":"section","location":"jlwinterop/#Owning-carrier-returns","page":"JLWInterop","text":"Libraries returning an owning CArray, CString, CStrArray, or CDict must emit the release entrypoints at module top level:\n\nusing JLWInterop\n\nJLWInterop.@export_release_entrypoints\n\nThe macro exports jlw_free and jlw_free_strings. Binding targets use these functions to release buffers through the library that allocated them. Owning buffers must be released exactly once; borrowed buffers must not be released.\n\nWithout these entrypoints, a target cannot safely automate owning returns. The current Python target leaves affected functions for manual wrapping. A library whose carriers are all borrowed needs no release entrypoints at all.","title":"Owning carrier returns"},{"category":"module","location":"jlwinterop/#JLWInterop.JLWInterop","page":"JLWInterop","text":"JLWInterop\n\nDependency-free ABI types for JuliaLibWrapping-generated libraries, plus @api, the annotation macro that generates a @ccallable wrapper (argument/return conversion, JLWResult/JLWStatus error reporting, and a build-host metadata registry) from an ordinary Julia function.\n\n\n\n\n\n","title":"JLWInterop.JLWInterop"},{"category":"type","location":"jlwinterop/#JLWInterop.JLWStatus","page":"JLWInterop","text":"JLWStatus\n\nABI status value. code == 0 means success; message is a fixed-size, NUL-terminated UTF-8 buffer.\n\n\n\n\n\n","title":"JLWInterop.JLWStatus"},{"category":"function","location":"jlwinterop/#JLWInterop.jlw_ok","page":"JLWInterop","text":"jlw_ok() -> JLWStatus\n\nReturn a success status: code 0 with an empty message buffer.\n\n\n\n\n\njlw_ok(value::C) -> JLWResult{C}\n\nWrap a successful value: status is jlw_ok() and value is value.\n\n\n\n\n\n","title":"JLWInterop.jlw_ok"},{"category":"function","location":"jlwinterop/#JLWInterop.jlw_error","page":"JLWInterop","text":"jlw_error(code::Integer, msg::AbstractString) -> JLWStatus\n\nReturn an allocation-free error status. code is converted to Int32; msg is truncated as needed and NUL-terminated.\n\n\n\n\n\njlw_error(code::Integer, msg::AbstractString, ::Type{C}) -> JLWResult{C}\n\nWrap a failure: status is jlw_error(code, msg) and value is a zero-filled C (see JLWInterop._zero_carrier) with a null pointer.\n\n\n\n\n\n","title":"JLWInterop.jlw_error"},{"category":"constant","location":"jlwinterop/#JLWInterop.JLW_MESSAGE_BYTES","page":"JLWInterop","text":"JLW_MESSAGE_BYTES\n\nSize of the inline JLWStatus message buffer. One byte is reserved for the terminating NUL.\n\n\n\n\n\n","title":"JLWInterop.JLW_MESSAGE_BYTES"},{"category":"type","location":"jlwinterop/#JLWInterop.CArray","page":"JLWInterop","text":"CArray{owned,T,N}\n\nColumn-major N-D buffer for @ccallable boundaries. data points to prod(dims) contiguous elements of T.\n\nOwnership contract\n\nowned is :owned or :borrowed, so whether a value must be released is a property of its type.\n\nCArray{:owned,T,N} holds Julia-allocated storage, produced by CArray{:owned}(::AbstractArray). The consumer releases data exactly once, with Libc.free or the jlw_free entrypoint emitted by @export_release_entrypoints.\n\nCArray{:borrowed,T,N} wraps memory the caller owns and keeps alive; the consumer never releases it, and must make the storage writable before mutating it. Pointer constructors build carriers of either ownership.\n\nExample\n\nusing JLWInterop\n\nBase.@ccallable function sum_values(a::CArray{:borrowed,Float64,2})::Float64\n    return sum(a)\nend\n\nExtended help\n\nT should be an isbits type. CArray implements the AbstractArray interface with linear indexing, including bounds-checked access and mutation. The aliases CVector and CMatrix cover one and two dimensions.\n\nEvery construction path names an ownership: there is no defaulting constructor, and any parameter other than :owned or :borrowed is rejected.\n\nBinding targets can map this layout to native N-D array types without changing the ABI. They must preserve column-major dimension and stride semantics. The two ownerships are two distinct types, so a target reads \"release this\" or \"do not release this\" off the signature alone.\n\n\n\n\n\n","title":"JLWInterop.CArray"},{"category":"type","location":"jlwinterop/#JLWInterop.CVector","page":"JLWInterop","text":"CVector{owned,T}\n\nAlias for CArray{owned,T,1}. See CArray.\n\n\n\n\n\n","title":"JLWInterop.CVector"},{"category":"type","location":"jlwinterop/#JLWInterop.CMatrix","page":"JLWInterop","text":"CMatrix{owned,T}\n\nAlias for CArray{owned,T,2}, laid out in column-major order. See CArray.\n\n\n\n\n\n","title":"JLWInterop.CMatrix"},{"category":"type","location":"jlwinterop/#JLWInterop.CString","page":"JLWInterop","text":"CString{owned}\n\nLength-prefixed UTF-8 string descriptor for @ccallable boundaries. It contains length bytes at data and permits embedded NUL bytes.\n\nOwnership contract\n\nowned is :owned or :borrowed.\n\nCString{:owned}(::AbstractString) allocates a copy. The consumer releases data once with Libc.free or the jlw_free entrypoint emitted by @export_release_entrypoints.\n\nCString{:borrowed} wraps a buffer the caller owns and keeps alive; the consumer never releases it.\n\nExample\n\nusing JLWInterop\n\nBase.@ccallable function greeting_length(s::CString{:borrowed})::Int32\n    return Int32(length(s))\nend\n\ns = CString{:owned}(\"héllo\")\nString(s) == \"héllo\"\nLibc.free(s.data)\n\nExtended help\n\nUnlike Base.Cstring, CString is length-prefixed rather than NUL-terminated.\n\nUse it instead of a String, which is not C-ABI compatible, or a Cstring, which requires NUL termination and forbids embedded NULs.\n\nEvery construction path names an ownership: there is no defaulting constructor, and any parameter other than :owned or :borrowed is rejected.\n\nCString{owned} <: AbstractString. Use String(s) to copy its bytes into a Julia String.\n\n\n\n\n\n","title":"JLWInterop.CString"},{"category":"type","location":"jlwinterop/#JLWInterop.CStrArray","page":"JLWInterop","text":"CStrArray{owned}\n\nArray of length-prefixed UTF-8 CStrings for C ABI boundaries.\n\nOwnership contract\n\nowned is :owned or :borrowed, so whether a value must be released is a property of its type rather than of the value.\n\nCStrArray{:owned} holds Julia-allocated storage, produced by CStrArray{:owned}(::Vector{String}). The consumer releases it exactly once with _free_strings or the jlw_free_strings entrypoint emitted by @export_release_entrypoints.\n\nCStrArray{:borrowed} wraps memory the caller owns and keeps alive; the consumer never releases it. Converting to Vector{String} copies without freeing the source.\n\nElements share the container's ownership — data points to CString{owned}s — and releasing an owning CStrArray releases every element's buffer too.\n\nThere is no default ownership or constructor that borrows a Vector{String}; borrowed carriers arrive across the ABI.\n\nExample\n\nusing JLWInterop\n\na = CStrArray{:owned}([\"hello\", \"world\"])\nVector{String}(a) == [\"hello\", \"world\"]\nJLWInterop._free_strings(a.data, a.length)\n\n\n\n\n\n","title":"JLWInterop.CStrArray"},{"category":"type","location":"jlwinterop/#JLWInterop.CDict","page":"JLWInterop","text":"CDict{owned,V}\n\nString-keyed dictionary for C ABI boundaries. Keys are length-prefixed CStrings and values are a parallel array of a type in CDICT_VALUE_TYPES.\n\nOwnership contract\n\nowned is :owned or :borrowed, so whether a value must be released is a property of its type.\n\nCDict{:owned,V} holds Julia-allocated storage, produced by CDict{:owned}(::Dict). The consumer releases keys with _free_strings and values with Libc.free, or uses the corresponding entrypoints emitted by @export_release_entrypoints, exactly once.\n\nCDict{:borrowed,V} wraps memory the caller owns and keeps alive; the consumer never releases it. Converting to a Dict copies without freeing the source.\n\nKeys share the dictionary's ownership — keys points to CString{owned}s — and releasing them releases every key's buffer too.\n\nThere is no default ownership or constructor that borrows a Dict; borrowed carriers arrive across the ABI.\n\nExample\n\nusing JLWInterop\n\nc = CDict{:owned}(Dict(\"a\" => 1.5, \"b\" => -2.0))\nDict{String,Float64}(c) == Dict(\"a\" => 1.5, \"b\" => -2.0)\nJLWInterop._free_strings(c.keys, c.length)\nLibc.free(c.values)\n\n\n\n\n\n","title":"JLWInterop.CDict"},{"category":"constant","location":"jlwinterop/#JLWInterop.CDICT_VALUE_TYPES","page":"JLWInterop","text":"CDICT_VALUE_TYPES\n\nSupported value types for CDict. The closed list permits concrete, trim-safe conversion methods; other value types throw MethodError.\n\n\n\n\n\n","title":"JLWInterop.CDICT_VALUE_TYPES"},{"category":"type","location":"jlwinterop/#JLWInterop.COpt","page":"JLWInterop","text":"COpt{T}\n\nC-ABI representation of Union{T,Nothing}. has_value is 1 when the inline value is present and 0 when it is absent. Absent values are zero-filled.\n\nConstruct with COpt(x) (present) or COpt{T}(nothing) (absent, zero-filled); read back with unwrap.\n\nExample\n\nusing JLWInterop\n\nunwrap(COpt(3.5)) === 3.5\nisnothing(unwrap(COpt{Float64}(nothing)))\n\n\n\n\n\n","title":"JLWInterop.COpt"},{"category":"function","location":"jlwinterop/#JLWInterop.unwrap","page":"JLWInterop","text":"unwrap(o::COpt{T}) -> Union{T,Nothing}\n\nConvert a COpt{T} to Union{T,Nothing}.\n\n\n\n\n\n","title":"JLWInterop.unwrap"},{"category":"macro","location":"jlwinterop/#JLWInterop.@export_release_entrypoints","page":"JLWInterop","text":"@export_release_entrypoints\n\nAt module top level, emit the release functions required by owning carrier returns. jlw_free frees one allocation; jlw_free_strings frees an array of CStrings and their buffers.\n\n\n\n\n\n","title":"JLWInterop.@export_release_entrypoints"},{"category":"function","location":"jlwinterop/#JLWInterop._free_strings","page":"JLWInterop","text":"JLWInterop._free_strings(p::Ptr{CString{:owned}}, n::Int64)\n\nFree n string buffers pointed to by the CStrings at p (each one's .data), then free p itself. Matches the allocation made by CStrArray{:owned}(::Vector{String}). Internal; exposed at a @ccallable boundary as jlw_free_strings by @export_release_entrypoints.\n\n\n\n\n\n","title":"JLWInterop._free_strings"},{"category":"section","location":"tutorial/#Tutorial:-wrap-an-OLS-regression-library","page":"Tutorial","text":"This tutorial builds examples/ols/, installs its generated Python package, and calls it with NumPy arrays.\n\nThe subject is ordinary least squares (OLS) regression, which exercises several JLWInterop types:\n\nJLWInterop type Where it appears in ols\nCMatrix{:borrowed,Float64} design matrix X\nCVector{:borrowed,Float64} response y, coefficients, predictions\nFloat64 (primitive) r_squared\nCString{:borrowed} output buffer for summary_report\nJLWStatus (direct) return of predict\nJLWStatus (embedded) FitResult.status field\n\nCMatrix and CVector are aliases for CArray specializations, and their leading :borrowed parameter says the library never owns these buffers. The example uses X \\ y from LinearAlgebra.","title":"Tutorial: wrap an OLS regression library"},{"category":"section","location":"tutorial/#1.-The-Julia-source","page":"Tutorial","text":"examples/ols/src/ols.jl defines three Base.@ccallable entrypoints. Note that all three of these take arguments with types defined in JLWInterop:\n\nmodule ols\n\nusing JLWInterop\nusing LinearAlgebra\n\nstruct FitResult\n    status::JLWStatus\n    coeffs::CVector{:borrowed, Float64}\n    r_squared::Float64\nend\n\nBase.@ccallable function fit(X::CMatrix{:borrowed, Float64},\n                              y::CVector{:borrowed, Float64},\n                              coeffs_buf::CVector{:borrowed, Float64})::FitResult\n    # … shape checks, then `coeffs = X \\\\ y`; copy into `coeffs_buf`,\n    # compute R^2, and return FitResult with JLWStatus(0,…) on success\n    # or JLWStatus(code, msg) on a recognized failure.\nend\n\nBase.@ccallable function predict(coeffs::CVector{:borrowed, Float64},\n                                  X::CMatrix{:borrowed, Float64},\n                                  out::CVector{:borrowed, Float64})::JLWStatus\n\nBase.@ccallable function summary_report(result::FitResult,\n                                         buf::CString{:borrowed})::JLWStatus\n\nSee examples/ols/src/ols.jl for the complete implementation.\n\ncoeffs_buf and out are caller-allocated buffers: the library writes into them but does not own them, which is what the :borrowed parameter records. summary_report's CString{:borrowed} buffer works the same way.\n\nErrors travel back as a JLWStatus, either returned directly (predict, summary_report) or embedded in a return struct (fit's FitResult). The Python emitter recognizes both forms and translates a non-zero code into a JLWError exception — see Error handling.","title":"1. The Julia source"},{"category":"section","location":"tutorial/#2.-The-entry-Project.toml","page":"Tutorial","text":"A minimal Project.toml for the library:\n\nname = \"ols\"\nuuid = \"7e81292c-b63a-42d7-9477-255b6fedc2ed\"\nversion = \"0.1.0\"\n\n[deps]\nJLWInterop = \"65e54657-ed21-41a3-96db-71ab7fa6d94b\"\nLinearAlgebra = \"37e2e46d-f89d-539d-b4ee-838fcccc9c8e\"\n\n[compat]\nJLWInterop = \"0.1\"\njulia = \"1.13\"\n\nRequirements:\n\nABI export requires Julia 1.13 or later.\nThe [deps] here describe what must be baked into ols.so. Keep it minimal, without build tooling or test dependencies. [sources] entries may use relative paths, such as path = \"..\" when this project is a subdirectory of the package it wraps. build_library handles juliac's project relocation without modifying the original project.","title":"2. The entry Project.toml"},{"category":"section","location":"tutorial/#3.-Build-the-library-and-Python-package","page":"Tutorial","text":"The build uses two environments:\n\nThe entry project (examples/ols/Project.toml, activated by julia --project=.) declares the runtime deps just described.\nA build-env (examples/ols/build-env/Project.toml) declares the build tooling — JuliaLibWrapping and JuliaC. build.jl pushes this directory onto LOAD_PATH so that using JuliaLibWrapping resolves there. Keeping the build tooling out of the entry project's [deps] keeps the library's runtime dependencies minimal.\n\nThe build-env's Project.toml is just:\n\n[deps]\nJuliaC = \"acedd4c2-ced6-4a15-accc-2607eb759ba2\"\nJuliaLibWrapping = \"d61f35a8-f6af-436f-bc10-cee6b101f7bd\"\n\n[compat]\nJuliaC = \"0.3\"\nJuliaLibWrapping = \"0.1\"\njulia = \"1.13\"\n\nRun the remaining commands from examples/ols.\n\nInstantiate each environment from your shell:\n\njulia --project=build-env -e 'using Pkg; Pkg.instantiate()'\njulia --project -e 'using Pkg; Pkg.instantiate()'\n\nThe build.jl script temporarily adds build-env to LOAD_PATH:\n\npush!(LOAD_PATH, joinpath(@__DIR__, \"build-env\"))\nusing JuliaLibWrapping, JuliaC\n\nstandard_build(@__DIR__; libname = \"ols\", verbose = true)\npop!(LOAD_PATH)\n\nusing JuliaC is what activates JuliaLibWrapping's weak dependency on JuliaC.jl; without it build_library errors with a hint pointing at this line.\n\nstandard_build is a convenience wrapper around build_library for the conventional layout — src/<libname>.jl as the entry, out/ as the artifact directory, both a C header and a bundled Python ctypes package named <libname>_py. For layouts outside that convention, or to drop one of the targets, call build_library directly; standard_build's docstring shows the equivalent expansion.\n\nThen run the build from your shell:\n\njulia --project=. build.jl\n\nAfter a successful build, out/ contains:\n\nout/\n├── ols.so              # compiled shared library\n├── ols.abi.json        # ABI metadata emitted by juliac\n├── ols.h               # C header (CTarget output)\n├── pyproject.toml      # Python package metadata\n└── ols_py/\n    ├── __init__.py\n    ├── _lowlevel.py    # generated ctypes bindings (regenerated every build)\n    ├── _facade.py      # public API (created once; user-editable)\n    └── bundle/         # juliac --bundle tree: libjulia, stdlibs, BLAS, …\n\nbundle = true is essential for a pip install user who has no Julia on their machine. See the bundling section of the overview for what is in the bundle tree and how the loader finds libjulia from inside the wheel.","title":"3. Build the library and Python package"},{"category":"section","location":"tutorial/#4.-Install-the-Python-package","page":"Tutorial","text":"Create a clean virtualenv with no system Julia, and install. Put the virtualenv outside the entry-project directory (here, /tmp/ols-venv): juliac copies the whole project tree on every build, and a venv nested inside it has symlinks that break that copy.\n\npython -m venv /tmp/ols-venv\nsource /tmp/ols-venv/bin/activate\npip install numpy\npip install -e ./out\n\nThe ./ selects a local path. -e makes later _facade.py edits available without reinstalling.\n\nThe bundled libjulia and stdlibs live inside the wheel; the loader in _lowlevel.py searches bundle/lib/ first so the baked-in RUNPATH resolves them at import time, with no LD_LIBRARY_PATH required.","title":"4. Install the Python package"},{"category":"section","location":"tutorial/#5.-Call-it-from-Python","page":"Tutorial","text":"","title":"5. Call it from Python"},{"category":"section","location":"tutorial/#Verify-the-generated-wrapper","page":"Tutorial","text":"First call predict, which is generated automatically because the emitter recognizes all of its argument and return types. The second call uses invalid input to demonstrate error handling:\n\nimport numpy as np\nfrom ols_py import predict, JLWError\n\ncoeffs = np.array([0.06, 1.98])\nX = np.asfortranarray(np.column_stack([np.ones(5), np.arange(1.0, 6.0)]))\nout = np.zeros(5)\npredict(coeffs, X, out)\n# out is now X @ coeffs\n\ntry:\n    bad = np.asfortranarray(np.zeros((5, 3)))   # wrong number of columns\n    predict(coeffs, bad, out)\nexcept JLWError as e:\n    print(e.code, e.message)   # 1, \"coeffs length must match X cols\"\n\nThe expected out is array([2.04, 4.02, 6., 7.98, 9.96]), followed by the error message.\n\nnp.asfortranarray is required for any CMatrix{owned,T} argument: JLWInterop's CArray is column-major, and the automatically created façade rejects a row-major view rather than silently transposing. You can edit the wrapper to accept a different interface.","title":"Verify the generated wrapper"},{"category":"section","location":"tutorial/#Edit-the-façade","page":"Tutorial","text":"In contrast with predict, fit is not automatically wrapped: it returns a FitResult, and JuliaLibWrapping declines to make choices about what that should look like from the Python perspective. The starter façade re-exports it from _lowlevel with a TODO: hand-wrap comment naming the obstacle. You edit _facade.py to provide the wrapper you want. The generated low-level layer still raises JLWError on a non-zero status, so a scikit-learn-style wrapper is:\n\n# in ols_py/_facade.py, replacing the auto-generated TODO line\ndef fit(X, y):\n    X = np.asfortranarray(X)\n    y = np.ascontiguousarray(y, dtype=np.float64)\n    coeffs = np.zeros(X.shape[1])\n    result = _lowlevel.fit(\n        _lowlevel.CMatrix_borrowed_Float64.from_numpy(X),\n        _lowlevel.CVector_borrowed_Float64.from_numpy(y),\n        _lowlevel.CVector_borrowed_Float64.from_numpy(coeffs),\n    )\n    return coeffs, result\n\nNote that fit accepts a plain (row-major) X and converts it with np.asfortranarray internally, so callers do not need the conversion required by predict. Alongside the coefficients, this wrapper returns the raw result struct, which carries r_squared and is what summary_report (next) consumes. A production wrapper might instead bundle these into a small result object exposing coeffs, r_squared, and a .summary() method; this returns the pieces directly to keep the example short.\n\nsummary_report is also a hand-wrap case (its FitResult argument is an unrecognized struct). The caller allocates a writable CString{:borrowed} buffer, passes it in, and decodes the bytes after the call:\n\ndef summary_report(result_struct, capacity=256):\n    import ctypes\n    buf_bytes = (ctypes.c_uint8 * capacity)()\n    buf = _lowlevel.CString_borrowed(\n        length=capacity,\n        data=ctypes.cast(buf_bytes, ctypes.POINTER(ctypes.c_uint8)),\n    )\n    _lowlevel.summary_report(result_struct, buf)\n    return bytes(buf_bytes).rstrip(b\"\\x00\").decode(\"utf-8\")","title":"Edit the façade"},{"category":"section","location":"tutorial/#Trying-out-fit-and-summary_report","page":"Tutorial","text":"With both wrappers in place, the two compose directly. fit is the inverse direction of predict: hand it a design matrix and observed responses, get back the coefficients. To make the result easy to check, we feed it the predictions from the predict call above as the observations — a perfectly linear y, so fit should recover the same [0.06, 1.98] with an R² of 1.0. Restart Python first so the _facade.py edits are picked up (the editable install means no reinstall is needed):\n\nimport numpy as np\nfrom ols_py import fit, summary_report\n\nX = np.column_stack([np.ones(5), np.arange(1.0, 6.0)])   # row-major is fine here\ny = np.array([2.04, 4.02, 6.0, 7.98, 9.96])              # the predictions from §5, now treated as data\n\ncoeffs, result = fit(X, y)\nprint(coeffs)                   # ≈ [0.06, 1.98] — the coefficients predict() used\nprint(result.r_squared)         # 1.0 (the points lie exactly on a line)\nprint(summary_report(result))   # \"OLS fit: 2 coefficients, R^2 = 1.0\"\n\nThe result returned by fit is the FitResult struct expected by summary_report. Try changing one entry of y and re-running: the coefficients shift slightly and result.r_squared drops below 1.0, which the summary string reflects.","title":"Trying out fit and summary_report"},{"category":"section","location":"tutorial/#6.-Adding-an-entrypoint-later","page":"Tutorial","text":"When you add a new Base.@ccallable to ols.jl:\n\n_lowlevel.py is regenerated on every write_wrapper / build_library call — your new entrypoint shows up automatically.\n_facade.py is written once and then never touched. To pick up new entrypoints in the starter façade, delete the file and rebuild; JuliaLibWrapping will regenerate it (auto-wrapping where it can, leaving # TODO: hand-wrap markers where it cannot).\n__init__.py is regenerated to re-export from _facade.\n\nYou trigger all of this by re-running the same build:\n\njulia --project=. build.jl\n\n_lowlevel.py, pyproject.toml, and __init__.py are rewritten in place. Because the package was installed with pip install -e, restart Python to pick up those changes without reinstalling.\n\nKeep _facade.py under version control alongside the build script. To generate wrappers for new functions, delete it on a branch, rebuild, and merge the relevant generated functions into the existing file.","title":"6. Adding an entrypoint later"},{"category":"section","location":"error_handling/#Error-handling-across-the-ABI","page":"Error handling","text":"A Julia throw inside a --trim-compiled @ccallable does not become a Python exception — at best it aborts the host process. JuliaLibWrapping defines a convention for reporting errors across the C ABI so that generated wrappers can translate them into native exceptions.","title":"Error handling across the ABI"},{"category":"section","location":"error_handling/#The-JLWStatus-convention","page":"Error handling","text":"The canonical status struct is defined in the JLWInterop subdir package and has the following shape:\n\nstruct JLWStatus\n    code::Int32                       # 0 == success; non-zero == error\n    message::NTuple{256, UInt8}       # UTF-8, null-terminated\nend\n\nA library opts in by either\n\nreturning a JLWStatus directly, or\nreturning a struct that contains a JLWStatus field (any field name).\n\nEither form is recognized by the Python backend.\n\nThe inline buffer keeps JLWStatus allocation-free and avoids pointer ownership concerns. Messages are bounded to 255 bytes plus a NUL terminator.","title":"The JLWStatus convention"},{"category":"section","location":"error_handling/#Authoring-a-library","page":"Error handling","text":"Add JLWInterop to your library's project, then construct status values with the provided helpers:\n\nusing JLWInterop\n\nstruct ResultStruct\n    status::JLWStatus\n    value::Float64\nend\n\nBase.@ccallable function compute(x::Float64)::ResultStruct\n    x < 0 && return ResultStruct(jlw_error(1, \"negative input\"), 0.0)\n    return ResultStruct(jlw_ok(), x * 2)\nend\n\njlw_ok() and jlw_error(code, msg) perform no heap allocation, so they are safe in --trim builds.","title":"Authoring a library"},{"category":"section","location":"error_handling/#What-the-Python-wrapper-does","page":"Error handling","text":"For each entrypoint whose return type carries a JLWStatus (directly or via an embedded field), the generated wrapper checks status.code and raises a JLWError (a RuntimeError subclass, carrying .code and .message) when it is non-zero:\n\nfrom mylib import compute, JLWError\n\ntry:\n    result = compute(-1.0)\nexcept JLWError as e:\n    print(e.code, e.message)   # 1, \"negative input\"\n\nOn success the wrapper returns the full struct unchanged, including the status field (so callers can still inspect it explicitly if they prefer).","title":"What the Python wrapper does"},{"category":"section","location":"error_handling/#C-backend","page":"Error handling","text":"The C header generator does not currently auto-emit a status-check macro; C callers should check result.status.code themselves and read the message with, for example, printf(\"%.256s\\n\", result.status.message). A JLW_CHECK-style helper macro is a possible follow-up.","title":"C backend"},{"category":"section","location":"#JuliaLibWrapping","page":"Home","text":"JuliaLibWrapping generates C headers and Python ctypes bindings for shared libraries compiled from Julia by juliac. It turns juliac ABI metadata into bindings for non-Julia callers.\n\nNew to the package? Start with the tutorial — it follows a small library from Julia source through pip install to a Python API using NumPy arrays.","title":"JuliaLibWrapping"},{"category":"section","location":"#The-two-tool-split","page":"Home","text":"juliac / JuliaC.jl --emits--> JSON ABI-info file --consumed by--> JuliaLibWrapping --emits--> .h / Python package\n\nJuliaC.jl compiles the shared library and emits a JSON file describing its ABI, but does not generate wrappers. JuliaLibWrapping consumes that JSON and emits the wrappers.\n\nbuild_library runs both stages in one call when JuliaC.jl is loaded. See Concepts for details about the pipeline, bundling, loading multiple libraries in one process, and generated Python files.","title":"The two-tool split"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Tutorial: wrap an OLS regression library: build a small library with a Python wrapper using numpy.\nConcepts: the pipeline, the ABI data model, the extension point for new target languages, and runtime bundling.\nJLWInterop: the package that defines shared ABI types (CArray, CString, and JLWStatus) for defining ABI-compatible Julia entrypoints.\nError handling across the ABI: the JLWStatus convention that lets wrapped libraries report errors as native exceptions in the target language.\nAPI reference: the public API.","title":"Where to go next"}]
}
