var documenterSearchIndex = {"docs":
[{"title":"API reference","page":"API reference","location":"api/#API-reference","category":"section","text":""},{"title":"JuliaLibWrapping.numpy_dtypes","page":"API reference","location":"api/#JuliaLibWrapping.numpy_dtypes","category":"constant","text":"numpy_dtypes :: Dict{String, String}\n\nMap from Julia primitive type name to the corresponding numpy dtype string. Used by the Python façade emitter to decide which pointer element types can be exposed as numpy arrays in CVector / CMatrix helpers; primitives absent from this table (notably Bool, the platform-aliased C ints) are not auto-wrapped.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.ABIInfo","page":"API reference","location":"api/#JuliaLibWrapping.ABIInfo","category":"type","text":"ABIInfo\n\nFields\n\ntypeinfo::OrderedDict{Int, TypeDesc}: A map from type_id to type_descriptor, sorted by declaration order.\nforward_declared::BitSet: Indexes into types, indicates which  types must be forward-declared for C.\nentrypoints::Vector{MethodDesc}: A vector of exposed functions  from the imported ABI.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.AbstractTarget","page":"API reference","location":"api/#JuliaLibWrapping.AbstractTarget","category":"type","text":"AbstractTarget\n\nSupertype of wrapper-emission targets. Each concrete subtype is a configuration struct describing where and how to emit one output language's bindings; a corresponding write_wrapper method consumes that configuration plus an ABIInfo and writes the files.\n\nShips today: CTarget for a C header, PythonTarget for a Python ctypes package. New languages are added by defining a subtype and a write_wrapper method for it.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.CTarget","page":"API reference","location":"api/#JuliaLibWrapping.CTarget","category":"type","text":"CTarget(dir, headerbase)\n\nOutput configuration for a C header. write_wrapper writes a single file joinpath(dir, headerbase * \".h\") containing typedefs for every struct in the ABI and extern declarations for every entrypoint.\n\nOnly the primitive Julia types listed in the emitter's ctypes table have a direct C mapping; an ABI referencing any other primitive raises an error. Pointer types are emitted inline as T* rather than as separate typedefs. Non-C-safe identifiers are scrubbed by sanitize_for_c and disambiguated with a numeric suffix on collision.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.PythonTarget","page":"API reference","location":"api/#JuliaLibWrapping.PythonTarget","category":"type","text":"PythonTarget(dir, package_name, library_basename; bundle_subdir = nothing)\n\nOutput configuration for a Python ctypes-based wrapper package. dir is the directory into which the package will be written; a sub-directory named package_name is created and is the importable Python module. library_basename is the shared library's basename without an OS-specific suffix (e.g. \"libsimple\", which will be loaded from libsimple.so / libsimple.dylib / libsimple.dll depending on the host).\n\nWhen bundle_subdir is a string (e.g. \"bundle\"), the emitter assumes the shared library and its juliac runtime closure (libjulia, sysimage, stdlibs, artifacts) will be laid out under that subdirectory of the Python package in the standard --bundle shape (<bundle_subdir>/lib/<lib>, <bundle_subdir>/lib/julia/, <bundle_subdir>/artifacts/). The generated loader looks for the library inside the bundle first, the generated pyproject.toml widens package-data to include the bundle tree, and build_library with bundle = true will copy the bundle there. The default nothing preserves the flat single-.so-next-to-the-package layout and is the right choice for callers placing the library by hand.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping._facade_classify_arg","page":"API reference","location":"api/#JuliaLibWrapping._facade_classify_arg-Tuple{JuliaLibWrapping.ArgDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}, Dict{Int64, String}}","category":"method","text":"_facade_classify_arg(arg, typeinfo, typedict) -> NamedTuple\n\nClassify a method argument for façade auto-wrapping. The return is one of:\n\n(kind=:primitive,) — pass-through\n(kind=:carray, classname=…) — wrap with <class>.from_numpy(name)\n(kind=:cstring, classname=…) — wrap with <class>.from_str(name)\n(kind=:opaque, reason=…) — bail out; emit mechanical re-export instead.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping._facade_classify_return","page":"API reference","location":"api/#JuliaLibWrapping._facade_classify_return-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}, Dict{Int64, String}}","category":"method","text":"_facade_classify_return(method, typeinfo, typedict) -> NamedTuple\n\nClassify a method's return for façade auto-wrapping. The return is one of:\n\n(kind=:passthrough,) — primitive scalar (including Cvoid)\n(kind=:carray_unwrap, classname=…) — return _result.as_numpy()\n(kind=:cstring_unwrap, classname=…) — return _result.as_str()\n(kind=:jlwstatus_discard,) — direct JLWStatus return; discard, return None\n(kind=:opaque, reason=…) — bail out.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping._facade_plan","page":"API reference","location":"api/#JuliaLibWrapping._facade_plan-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}, Dict{Int64, String}}","category":"method","text":"_facade_plan(method, typeinfo, typedict) -> NamedTuple\n\nDecide whether an entrypoint should be auto-wrapped on the façade. Returns (auto::Bool, reason::String, args::Vector, ret::NamedTuple, uses_numpy::Bool).\n\nA function is auto-wrapped only when every argument and return classifies as a recognized form and the wrapping actually adds value (converts a vocabulary type or strips a discardable JLWStatus). Plain primitive-in/primitive-out functions are left as straight re-exports.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.build_library","page":"API reference","location":"api/#JuliaLibWrapping.build_library-Tuple{AbstractString, AbstractVector{<:AbstractTarget}}","category":"method","text":"build_library(entry, targets;\n              project=dirname(entry), libname, libdir=pwd(),\n              abi_path=joinpath(libdir, libname*\".abi.json\"),\n              trim=:safe, compile_ccallable=true,\n              backend=:auto, verbose=false,\n              bundle=false, bundle_dir=joinpath(libdir, libname*\"-bundle\"),\n              privatize=false)\n\nRun the full juliac → ABI JSON → wrapper pipeline in one call.\n\nentry is the path to the Julia source file (or package directory) that juliac will compile; targets is a vector of AbstractTargets that will each receive a write_wrapper call once the ABI JSON is available.\n\nReturns a NamedTuple (library, abi_path, abi_info, target_outputs, backend, bundle_dir). bundle_dir is the path to the produced bundle tree when bundle = true, and nothing otherwise.\n\nBackend\n\nbuild_library drives JuliaC.jl (a weak dependency); load it with using JuliaC before calling this function. backend = :auto (the default) and backend = :juliac are synonyms; the kwarg is retained so additional backends can be added without breaking the call surface.\n\nExample\n\nusing JuliaLibWrapping\nout = mktempdir()\nresult = build_library(\n    joinpath(@__DIR__, \"src/mylib.jl\"),\n    [CTarget(out, \"mylib\"), PythonTarget(out, \"mylib_py\", \"mylib\")];\n    project = @__DIR__,\n    libname = \"mylib\",\n    libdir  = out,\n)\n\n[sources] paths must be absolute\n\njuliac relocates the project into a temporary directory before compiling. Relative [sources] paths in the entry project's Project.toml cannot be resolved from there, so this function rejects them up front. Either use absolute paths or Pkg.develop the dependency.\n\nBundling for distribution (issue #17)\n\nA juliac-produced .so depends on libjulia, a sysimage, stdlibs, and artifacts — none of which a pip install-ing Python user has on their machine. Pass bundle = true to also produce a self-contained directory tree (the juliac --bundle layout) and copy it into every PythonTarget's package. The Python loader generated for those targets searches the bundle first, so the baked-in RUNPATH resolves libjulia from inside the wheel at import time.\n\nbundle = true requires the :juliac backend and that each PythonTarget declare a bundle_subdir (e.g. PythonTarget(out, \"mylib_py\", \"mylib\"; bundle_subdir = \"bundle\")). Targets that are not Python (e.g. CTarget) are unaffected — C consumers manage their own linkage.\n\nprivatize = true salts the bundled libjulia files with a random prefix so they cannot collide with a system libjulia. Off by default; opt in if the wrapper might be loaded into a process that also has a different libjulia.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.carray_struct_info","page":"API reference","location":"api/#JuliaLibWrapping.carray_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","category":"method","text":"carray_struct_info(desc::StructDesc, typeinfo) -> Union{Nothing, NamedTuple}\n\nRecognize the JLWInterop CArray{T,N} shape (which subsumes CVector{T} = CArray{T,1} and CMatrix{T} = CArray{T,2}): a struct whose name starts with \"CArray\", \"CVector\", or \"CMatrix\", with exactly two fields named dims (a fixed-size array of N integers, i.e. an ArrayDesc of a signed/unsigned integer primitive in numpy_dtypes) and data (a pointer to a primitive numeric type also in numpy_dtypes). Field order may be either dims, data or data, dims. Returns (; pointee_name, pointee_ctype, dtype, ndim) on a match (with ndim set to the dims array's count), otherwise nothing.\n\nLike is_jlwstatus_struct, recognition is by name + shape so authors who copy-paste a compatible definition still get the behavior.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.cstring_struct_info","page":"API reference","location":"api/#JuliaLibWrapping.cstring_struct_info-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","category":"method","text":"cstring_struct_info(desc::StructDesc, typeinfo) -> Bool\n\nRecognize the JLWInterop CString shape: a struct whose name starts with \"CString\", with exactly two fields named length (a primitive integer) and data (a pointer to UInt8). The pointee type is restricted to UInt8 specifically (other widths would not round-trip as a UTF-8 string). Returns true on a match, false otherwise. Field order may be either length, data or data, length. Recognition is by name + shape (see is_jlwstatus_struct for the rationale).\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.is_jlwstatus_struct","page":"API reference","location":"api/#JuliaLibWrapping.is_jlwstatus_struct-Tuple{JuliaLibWrapping.StructDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","category":"method","text":"is_jlwstatus_struct(desc::StructDesc, typeinfo) -> Bool\n\nRecognize the JLWInterop error-status convention by structural shape: a struct named JLWStatus with two fields — an integer code field and a message field that is a fixed-size byte array. Matching by name + shape (rather than by package identity) means authors who copy-paste a compatible definition still get the behavior.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.jlwstatus_access_path","page":"API reference","location":"api/#JuliaLibWrapping.jlwstatus_access_path-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","category":"method","text":"jlwstatus_access_path(method, typeinfo) -> Union{Nothing, String}\n\nIf method's return type carries a JLWStatus (either the return type is a JLWStatus or it is a struct with a JLWStatus field), return the Python attribute path from _result to that status (e.g. \"\" for direct return, or \".status\" for an embedded field). Otherwise return nothing. Recognition is shallow on purpose — only the immediate return struct's top-level fields are inspected.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.mangle_python!","page":"API reference","location":"api/#JuliaLibWrapping.mangle_python!-Tuple{Dict{Int64, String}, Int64, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","category":"method","text":"mangle_python!(typedict, type_id, typeinfo) -> String\n\nReturn a Python expression naming the ctypes type for type_id. Struct names go through sanitize_for_c (whose output is also a valid Python identifier) with a _<id> collision suffix matching mangle_c!. Pointer types render inline as ctypes.POINTER(...); Ptr{Cvoid} collapses to ctypes.c_void_p. Array types render inline as (<eltype> * N). Results are memoized in typedict.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.parse_abi_info","page":"API reference","location":"api/#JuliaLibWrapping.parse_abi_info-Tuple{AbstractDict}","category":"method","text":"abi_info = parse_abi_info(parsed::AbstractDict)\n\nBuild an ABIInfo from a parsed juliac ABI-info JSON document. parsed is the dictionary returned by JSON.parsefile (or JSON.parse) on such a file.\n\nSee read_abi_info for the file-based convenience.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.raw_primitive_pointer_args","page":"API reference","location":"api/#JuliaLibWrapping.raw_primitive_pointer_args-Tuple{JuliaLibWrapping.MethodDesc, OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","category":"method","text":"raw_primitive_pointer_args(method::MethodDesc, typeinfo) -> Vector{Int}\n\nReturn positional indices into method.args for arguments whose static type is a bare Ptr{T} where T is a primitive numeric type recognized by numpy_dtypes. Ptr{Cvoid} is excluded (it lowers to ctypes.c_void_p). Pointers wrapped inside CArray / CString structs are not reported — only top-level argument types are examined.\n\nA non-empty result signals an argument that hands the C function a raw memory address with no length, ownership, or layout metadata. The Python emitter uses this to attach a docstring on the wrapper noting the column-major contract and recommending the JLWInterop.CArray vocabulary instead.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.read_abi_info","page":"API reference","location":"api/#JuliaLibWrapping.read_abi_info-Tuple{AbstractString}","category":"method","text":"abi_info = read_abi_info(filename::AbstractString)\nabi_info = read_abi_info(io::IO)\n\nRead and parse a juliac ABI-info JSON file, returning an ABIInfo. The first form is equivalent to parse_abi_info(JSON.parsefile(filename)); the second reads the document from a stream.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.sanitize_for_c","page":"API reference","location":"api/#JuliaLibWrapping.sanitize_for_c-Tuple{AbstractString}","category":"method","text":"sanitize_for_c(str) -> String\n\nReturn str with all non-alphanumeric (non-underscore) characters replaced by _, leading/trailing underscores stripped, and runs of underscores collapsed. Used to coerce Julia identifiers and type names into valid C tokens. Two distinct inputs may collide; callers that need uniqueness (e.g. mangle_c!) suffix the result with a numeric disambiguator.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.sanitize_python_argname","page":"API reference","location":"api/#JuliaLibWrapping.sanitize_python_argname","category":"function","text":"sanitize_python_argname(name) -> String\nsanitize_python_argname(name, seen::Set{String}) -> String\n\nReturn a Python-identifier form of name: characters illegal in identifiers are stripped via sanitize_for_c, an empty result becomes \"_\", a leading digit (legal in juliac-emitted tuple field names like \"1\", \"2\", …, but illegal in a Python identifier) is prefixed with _, and any reserved Python keyword is suffixed with _.\n\nWhen a seen set is supplied, the returned name is also made unique within that scope (callers should pass one Set{String} per scope — e.g. one per function signature, one per struct's field list). If the candidate already appears in seen, an integer suffix (2, 3, …) is appended until the name is fresh, skipping any value that itself already collides — so the result is safe even when sanitized input happens to look like another argument plus a numeric tail. The chosen name is inserted into seen before returning.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.sort_declarations!","page":"API reference","location":"api/#JuliaLibWrapping.sort_declarations!-Tuple{OrderedCollections.OrderedDict{Int64, Union{JuliaLibWrapping.ArrayDesc, JuliaLibWrapping.PointerDesc, JuliaLibWrapping.PrimitiveTypeDesc, JuliaLibWrapping.StructDesc}}}","category":"method","text":"sort_declarations!(typedescs) -> forward_declarations\n\nSort typedescs w.r.t. type-dependencies (e.g. Type A using Type B in a field), so that a type-descriptor always appears after any dependencies. Sort is performed in-place.\n\nReturns indices of all types that could not be sorted (due to recursive types, e.g. a linked list in C). In C, these are the definitions that must be forward-declared.\n\n\n\n\n\n"},{"title":"JuliaLibWrapping.write_wrapper","page":"API reference","location":"api/#JuliaLibWrapping.write_wrapper","category":"function","text":"write_wrapper(target::AbstractTarget, abi_info::ABIInfo)\n\nEmit wrapper source files for abi_info into the location described by target. The methods that ship are dispatched on CTarget (one .h file) and PythonTarget (a Python ctypes package directory). Add a method for a new AbstractTarget subtype to support another output language.\n\n\n\n\n\n"},{"title":"Concepts","page":"Concepts","location":"concepts/#Concepts","category":"section","text":"This page describes the pipeline architecture, the data model that flows through it, and the cross-cutting concerns (bundling, runtime sharing, two-tier output) that shape the generated wrappers."},{"title":"The pipeline","page":"Concepts","location":"concepts/#The-pipeline","category":"section","text":"The transformation from a Julia source file to a wrapper package runs in three stages with an optional driver in front:\n\nsource.jl\n  │   juliac\n  ▼\nlib<name>.so + <name>_abi.json\n  │   read_abi_info → parse_abi_info → sort_declarations!\n  ▼\nABIInfo (descriptors in dependency order)\n  │   write_wrapper(target, abi_info)\n  ▼\n.h / Python package\n\nbuild_library chains the whole sequence; the stages are also callable individually when you want finer control or are testing the emitters against fixture JSON."},{"title":"Driving the pipeline","page":"Concepts","location":"concepts/#Driving-the-pipeline","category":"section","text":"build_library runs the full juliac → ABI JSON → wrapper pipeline in one call:\n\nusing JuliaLibWrapping\nusing JuliaC\nout = mktempdir()\nresult = build_library(\n    joinpath(@__DIR__, \"src/mylib.jl\"),\n    [CTarget(out, \"mylib\"), PythonTarget(out, \"mylib_py\", \"mylib\")];\n    project = @__DIR__,\n    libname = \"mylib\",\n    libdir  = out,\n)\n\nbuild_library invokes juliac to produce the shared library and ABI JSON, then applies write_wrapper to each target. The pipeline is driven through JuliaC.jl (a weak dependency): load it with using JuliaC before calling build_library.\n\nRelative [sources] paths in the entry project's Project.toml are rejected up front, because juliac relocates the project into a temporary directory before compiling."},{"title":"The ABI data model","page":"Concepts","location":"concepts/#The-ABI-data-model","category":"section","text":"juliac assigns every type an integer type_id. The JSON file contains a flat list of type descriptors keyed by that id, plus a list of exported entrypoint methods.\n\nAfter parsing, an ABIInfo carries an OrderedDict{Int, TypeDesc} of descriptors:\n\nPrimitiveTypeDesc — Int32, Float64, etc.\nStructDesc — a sequence of FieldDescs referencing other type ids.\nPointerDesc — a pointer to another type id.\n\nand a list of MethodDescs whose ArgDescs likewise reference types by id. All cross-references use ids, not names; the descriptors form a graph.\n\nsort_declarations! is the conceptual core of the import stage. C requires a type to be defined before use, so the dict must be sorted into dependency order. The implementation builds the type-dependency graph (Graphs.jl), finds strongly-connected components for mutually-recursive types, drops the pointer edges within those SCCs to make the graph acyclic, topologically sorts the result, and returns the forward_declared::BitSet that the C emitter uses to insert forward declarations."},{"title":"Emission targets","page":"Concepts","location":"concepts/#Emission-targets","category":"section","text":"AbstractTarget is the extension point for new output formats. Each concrete target is a configuration struct passed to write_wrapper(target, abi_info). Two backends ship today:\n\nCTarget — emits a single .h header. Primitive types map through a fixed ctypes table; non-primitive names go through mangle_c! to produce C-safe identifiers (memoized in a per-target typedict, with numeric suffixing on collision). Pointer types are emitted inline as T* rather than as separate typedefs.\nPythonTarget — emits a Python ctypes package with the two-tier layout described below.\n\nAdding a new target means defining a struct subtype of AbstractTarget and a method write_wrapper(::YourTarget, ::ABIInfo) that walks the sorted descriptors and emits whatever your target language requires."},{"title":"Two-tier Python output","page":"Concepts","location":"concepts/#Two-tier-Python-output","category":"section","text":"write_wrapper(PythonTarget, …) emits three files into the generated package directory:\n\n_lowlevel.py — the mechanical ctypes bindings: Structure subclasses and functions carrying the raw C signature. Always regenerated on every write_wrapper call.\n_facade.py — the idiomatic surface that consumers of the package actually call. JuliaLibWrapping writes this once as a starter façade, then never touches it again. The starter is not a blank stub: any entrypoint whose arguments and return are all recognized — primitive scalars, CVector{T}, CMatrix{T}, CString, or a direct JLWStatus return — is auto-wrapped to accept and return idiomatic Python objects (numpy arrays, str). Entrypoints with a raw pointer, an unrecognized struct, or an embedded JLWStatus field are re-exported from _lowlevel and tagged with a # TODO: hand-wrap comment naming the obstacle. Edit anything freely; to regenerate (for example, after adding new entrypoints), delete the file and re-run write_wrapper.\n__init__.py — always regenerated; re-exports from _facade so the façade is the package's public surface.\n\nThe intent is that scientific users of the wrapped library import the package top-level and see only the façade. The mechanical layer remains available under pkg._lowlevel for power users who need it.\n\nRecognition of CVector / CMatrix / CString / JLWStatus is structural — by struct name plus field shape — so authors who copy-paste a compatible definition into their own library still get the same wrapper behavior. JLWInterop is the canonical source of these definitions; using it keeps libraries from drifting out of structural compatibility."},{"title":"Bundling for distribution","page":"Concepts","location":"concepts/#Bundling-for-distribution","category":"section","text":"A juliac-compiled .so is not self-contained: it links against libjulia, depends on a sysimage, and pulls in stdlibs and JLL artifacts. A Python user who runs pip install does not have any of that on their machine, so the default flat lib<name>.so-next-to-the- package layout fails at import time for the actual target audience.\n\nPass bundle = true to build_library to also assemble the full runtime closure (the juliac --bundle layout) and copy it into every PythonTarget's package:\n\nusing JuliaLibWrapping, JuliaC\nout = mktempdir()\nresult = build_library(\n    joinpath(@__DIR__, \"src/mylib.jl\"),\n    [PythonTarget(out, \"mylib_py\", \"mylib\"; bundle_subdir = \"bundle\")];\n    project = @__DIR__,\n    libname = \"mylib\",\n    libdir  = out,\n    bundle  = true,\n)\n\nThe resulting package layout is\n\nout/mylib_py/\n├── __init__.py\n├── _facade.py\n├── _lowlevel.py\n├── pyproject.toml\n└── bundle/\n    ├── lib/\n    │   ├── libmylib.so       # user lib, RUNPATH=$ORIGIN/[/julia]\n    │   ├── libjulia.so.1.13\n    │   └── julia/…           # libjulia-internal, stdlibs, BLAS, …\n    └── artifacts/…\n\nThe generated _lowlevel.py loader searches bundle/lib/ before the package root, so the RUNPATH baked in at link time resolves libjulia and friends from inside the wheel — no LD_LIBRARY_PATH and no Julia install required on the user's machine. A developer who instead drops a bare lib<name>.so next to the package (without bundling) still works: the loader falls back to the flat layout.\n\npip install ./out/mylib_py from a clean virtualenv on a machine without a system Julia is the right manual check.\n\nbundle = true requires the :juliac backend and that each PythonTarget declare a bundle_subdir. The bundle itself is multi-hundred-MB (mostly libLLVM and libjulia-codegen), so it is opt-in. Pass privatize = true to additionally salt the bundled libjulia files with a random prefix, avoiding any chance of collision with a system libjulia loaded by the same process.\n\nWheel-level packaging (platform tags, manylinux audit) is out of scope for the current MVP — pyproject.toml builds a generic sdist/wheel and the developer is responsible for any platform tagging the distribution target requires."},{"title":"Multiple wrapped libraries in one process","page":"Concepts","location":"concepts/#Multiple-wrapped-libraries-in-one-process","category":"section","text":"One JLW-wrapped library per Python process is the supported configuration. If your users need to combine two Julia libraries, compile them as a single juliac library that exports both APIs.\n\nThe reason is that juliac libraries embed libjulia as a runtime dependency, and the Julia runtime is not designed to coexist with another copy of itself in the same process. With the default bundle layout each wheel ships its own bundle/lib/libjulia.so.1.13, but the dynamic linker satisfies the second library's DT_NEEDED libjulia.so.1.13 with the first library's already-loaded copy — first one wins. That silently \"works\" only when both libraries were built against byte-compatible Julia versions and their sysimages don't collide on global runtime state; mismatched versions may crash or miscompute.\n\nPassing privatize = true to build_library salts each bundle's libjulia with a random SONAME prefix so the dynamic linker maps both copies independently. That removes the silent-sharing footgun at the linker layer, but two Julia runtimes in one process — independent GC root sets, two thread pools, two BLAS trampoline initializations, two signal handler registrations — is itself untested territory. We have no evidence the combination is robust.\n\nTo make the situation loud rather than silent, every generated _lowlevel.py records its package name on a process-global sentinel (sys._jlw_loaded_packages) at import time and emits a RuntimeWarning when a second JLW-wrapped package is imported into the same process. Tracked as issue #28."},{"title":"JLWInterop","page":"JLWInterop","location":"jlwinterop/#JLWInterop","category":"section","text":""},{"title":"Ownership and layout discipline","page":"JLWInterop","location":"jlwinterop/#Ownership-and-layout-discipline","category":"section","text":"Every type in this package holds a raw pointer and does not own the underlying storage. The caller that allocated the buffer is responsible for keeping it alive for the duration of any call that sees it, and for freeing it afterward. In exchange, every type is isbits (when the element type is), is juliac --trim-friendly, and crosses a @ccallable boundary without heap allocation.\n\nThe JuliaLibWrapping Python emitter recognizes these types structurally — by struct name plus field shape — so an author who copy-pastes a compatible definition into their own library gets the same wrapper behavior. Depending on JLWInterop is the way to keep the definitions from drifting across libraries."},{"title":"JLWStatus — in-band error reporting","page":"JLWInterop","location":"jlwinterop/#JLWStatus-—-in-band-error-reporting","category":"section","text":"A library that needs to surface errors to its caller (rather than abort the process) returns either a JLWStatus directly, or a struct that contains a JLWStatus field. code == 0 is success; any non-zero value is an error code the library defines. message is a fixed-size UTF-8 buffer, null-terminated within the buffer.\n\nThe buffer is inline and fixed-size (JLW_MESSAGE_BYTES bytes) on purpose: a Cstring or Ptr{UInt8} would force a decision about who allocates and frees the message, which has no good answer under juliac --trim. The price is a bounded message length; the benefit is that constructing a status performs no heap allocation.\n\nConstruct values with the helpers:\n\nusing JLWInterop\n\nBase.@ccallable function safe_sqrt(x::Float64)::JLWStatus\n    x < 0 && return jlw_error(1, \"negative input\")\n    return jlw_ok()\nend\n\nSee Error handling across the ABI for the full library-author-and-Python-caller round trip, including how the emitter raises JLWError on the Python side."},{"title":"CArray{T,N} — N-D numeric buffer (column-major)","page":"JLWInterop","location":"jlwinterop/#CArray{T,N}-—-N-D-numeric-buffer-(column-major)","category":"section","text":"CArray{T,N} is (dims::NTuple{N,Int32}, data::Ptr{T}), laid out in column-major order — the same convention as Array{T,N} and Fortran, not C. The caller owns the storage; CArray borrows. For primitive numeric T, the Python emitter generates from_numpy / as_numpy helpers so a Python caller can pass a numpy.ndarray directly without copying.\n\nThe 1-D and 2-D specializations have familiar aliases:\n\nconst CVector{T} = CArray{T,1}\nconst CMatrix{T} = CArray{T,2}\n\nmirroring Julia's own Vector{T} = Array{T,1} / Matrix{T} = Array{T,2}. You can use either the alias or the underlying CArray{T,N} form; they are the same type.\n\nThe column-major choice has a sharp consequence on the Python side for N ≥ 2: the generated from_numpy helper requires a Fortran-contiguous array and rejects a default row-major ndarray. Silently treating a row-major buffer as column-major would transpose without warning, which is a footgun no caller asks for. Python callers wrapping a default numpy array must .copy(order='F') (or np.asfortranarray(arr)) first.\n\nCArray{T,N} <: AbstractArray{T,N} with IndexLinear() style, so the type participates in iteration, broadcasting, sum, views, and any function that accepts an AbstractArray{T,N} — at zero allocation. setindex! is defined unconditionally; only call it on storage you know to be writable."},{"title":"CString — length-prefixed UTF-8","page":"JLWInterop","location":"jlwinterop/#CString-—-length-prefixed-UTF-8","category":"section","text":"CString is (length::Int32, data::Ptr{UInt8}). It is length-prefixed and not null-terminated — embedded NUL bytes are permitted; length is the authoritative size. This makes it distinct from Base.Cstring, which is null-terminated and forbids embedded NULs.\n\nAs with the other types, CString borrows storage from the caller. The Python emitter recognizes it by name plus shape and generates from_str / as_str (UTF-8 round-trip) plus from_bytes / as_bytes (raw bytes) helpers.\n\nCString <: AbstractString with ncodeunits, codeunit, valid- position checking, UTF-8 iteration, and a fast byte-level cmp. Base derives the rest: length (character count, distinct from ncodeunits), ==, print, regex matching, split, replace, … Call String(s) to copy the bytes out into a fresh, heap-allocated, owning Julia String."},{"title":"JLWInterop.JLWInterop","page":"JLWInterop","location":"jlwinterop/#JLWInterop.JLWInterop","category":"module","text":"JLWInterop\n\nCanonical types for JuliaLibWrapping cross-ABI conventions. Provides JLWStatus for in-band error reporting and CArray (with CVector and CMatrix aliases) for N-D numeric buffers; both are designed to cross a @ccallable boundary.\n\nA library author opts in by using these types in any @ccallable-returned or @ccallable-accepted struct; the JuliaLibWrapping Python emitter then recognizes them by struct name + shape and emits idiomatic code — raising a native Python exception on a non-zero JLWStatus.code, and exposing from_numpy / as_numpy helpers on each recognized CArray.\n\nThis package deliberately has no dependencies so it stays juliac --trim-friendly: it is intended to be a build-time and runtime dependency of compiled libraries.\n\n\n\n\n\n"},{"title":"JLWInterop.JLWStatus","page":"JLWInterop","location":"jlwinterop/#JLWInterop.JLWStatus","category":"type","text":"JLWStatus\n\nABI-stable status struct. code == 0 means success; any non-zero value is an error code chosen by the library. message is a UTF-8, null-terminated buffer of fixed length (JLW_MESSAGE_BYTES bytes). The fixed buffer avoids the ownership/lifetime question that Cstring/Ptr{UInt8} would raise under --trim; payload size is bounded but no allocation is required to construct one.\n\n\n\n\n\n"},{"title":"JLWInterop.jlw_ok","page":"JLWInterop","location":"jlwinterop/#JLWInterop.jlw_ok","category":"function","text":"jlw_ok() -> JLWStatus\n\nReturn a success status: code 0 with an empty message buffer.\n\n\n\n\n\n"},{"title":"JLWInterop.jlw_error","page":"JLWInterop","location":"jlwinterop/#JLWInterop.jlw_error","category":"function","text":"jlw_error(code::Integer, msg::AbstractString) -> JLWStatus\n\nReturn an error status with the given non-zero code and msg. msg is copied into the fixed-size buffer, truncated to JLW_MESSAGE_BYTES - 1 bytes if necessary, and always null-terminated. code is converted to Int32.\n\nConstructing this status performs no heap allocation.\n\n\n\n\n\n"},{"title":"JLWInterop.JLW_MESSAGE_BYTES","page":"JLWInterop","location":"jlwinterop/#JLWInterop.JLW_MESSAGE_BYTES","category":"constant","text":"JLW_MESSAGE_BYTES\n\nThe fixed size, in bytes, of the inline message buffer inside JLWStatus. Sets the maximum message length an error can carry across the ABI; longer strings passed to jlw_error are truncated to JLW_MESSAGE_BYTES - 1 bytes plus a terminating NUL.\n\n\n\n\n\n"},{"title":"JLWInterop.CArray","page":"JLWInterop","location":"jlwinterop/#JLWInterop.CArray","category":"type","text":"CArray{T,N}\n\nABI-stable N-D buffer descriptor for crossing a @ccallable boundary: prod(dims) contiguous elements of type T starting at data, laid out in column-major order (the same convention as Julia's Array{T,N} and Fortran). T should be an isbits type, in which case CArray{T,N} is itself isbits and allocation-free to construct, keeping the type juliac --trim-friendly. The dims field is NTuple{N,Int32}, matching the rest of the JLWInterop ABI vocabulary.\n\nThe 1-D and 2-D specializations have familiar aliases:\n\nconst CVector{T} = CArray{T,1}\nconst CMatrix{T} = CArray{T,2}\n\nCArray{T,N} holds a raw pointer; it does not own the underlying buffer. Whoever allocated the storage (the caller passing the buffer in, or the library that returned it) remains responsible for keeping it alive for the entire time any CArray referring to it is in use, and for freeing it afterward. CArray performs no allocation, no copy, and no finalization.\n\nUse it to expose a numeric buffer at a @ccallable boundary instead of a Vector/Matrix/Array (which are not C-ABI compatible). The JuliaLibWrapping Python emitter recognizes CArray{T,N} for primitive numeric T and generates from_numpy / as_numpy helpers on the corresponding ctypes.Structure. For N ≥ 2 the storage is column-major, so from_numpy requires a Fortran-contiguous numpy.ndarray and rejects the default row-major layout: callers passing a default numpy array must write np.asfortranarray(arr) (or equivalent) first. This is deliberate — silently treating a row-major array as column-major would reinterpret the data without warning.\n\nCArray{T,N} <: AbstractArray{T,N} and implements size, bounds-checked getindex, and setindex! via unsafe_load / unsafe_store! on data, with IndexLinear() style over the column-major storage. CArray participates in iteration, broadcasting, sum, views, LinearAlgebra routines, and any function that accepts an AbstractArray{T,N} — at zero allocation. setindex! is defined unconditionally, so callers must only invoke it on buffers they know to be writable.\n\nExample\n\nusing JLWInterop\n\nBase.@ccallable function sum_cvector(v::CVector{Float64})::Float64\n    return sum(v)\nend\n\nBase.@ccallable function trace_cmatrix(m::CMatrix{Float64})::Float64\n    n = min(size(m, 1), size(m, 2))\n    s = 0.0\n    @inbounds for i in 1:n\n        s += m[i, i]\n    end\n    return s\nend\n\nBase.@ccallable function sum3d(a::CArray{Float64,3})::Float64\n    return sum(a)\nend\n\nThe caller (in Julia, Python, or C) is responsible for ensuring a.data points to at least prod(a.dims) valid T slots, in column-major order, for the duration of the call, and that the slots are writable when setindex! is used.\n\n\n\n\n\n"},{"title":"JLWInterop.CVector","page":"JLWInterop","location":"jlwinterop/#JLWInterop.CVector","category":"type","text":"CVector{T}\n\nAlias for CArray{T,1}. See CArray.\n\n\n\n\n\n"},{"title":"JLWInterop.CMatrix","page":"JLWInterop","location":"jlwinterop/#JLWInterop.CMatrix","category":"type","text":"CMatrix{T}\n\nAlias for CArray{T,2}, laid out in column-major order. See CArray.\n\n\n\n\n\n"},{"title":"JLWInterop.CString","page":"JLWInterop","location":"jlwinterop/#JLWInterop.CString","category":"type","text":"CString\n\nABI-stable byte-string descriptor for crossing a @ccallable boundary: length bytes starting at data, interpreted as UTF-8. Length-prefixed, not null-terminated — embedded NUL bytes are permitted and length is the authoritative size. Distinct from Base.Cstring (which is null-terminated).\n\nCString holds a raw pointer; it does not own the underlying buffer. Whoever allocated the storage (the caller passing the string in, or the library that returned it) remains responsible for keeping it alive for the entire time any CString referring to it is in use, and for freeing it afterward. CString performs no allocation, no copy, and no finalization.\n\nUse it to expose a string value at a @ccallable boundary instead of a String (which is not C-ABI compatible) or a Cstring (which forces null-termination and forbids embedded NULs). The JuliaLibWrapping Python emitter recognizes CString by name + shape and emits from_str / as_str (UTF-8) plus from_bytes / as_bytes (raw) helpers on the corresponding ctypes.Structure.\n\nCString <: AbstractString with ncodeunits, codeunit, and a fast byte-level cmp; Base derives UTF-8 iteration, length (character count vs. ncodeunits byte count), equality, print, regex matching, split, replace, and the rest of the AbstractString interface. Use String(s) to copy the bytes out into a fresh heap-allocated Julia String when you need ownership.\n\nExample\n\nusing JLWInterop\n\nBase.@ccallable function greeting_length(s::CString)::Int32\n    # AbstractString gives `length` (character count) directly; `s.length`\n    # is the byte count.\n    return Int32(length(s))\nend\n\nThe caller (in Julia, Python, or C) is responsible for ensuring s.data points to at least s.length valid UTF-8 bytes for the duration of the call.\n\n\n\n\n\n"},{"title":"Error handling across the ABI","page":"Error handling","location":"error_handling/#Error-handling-across-the-ABI","category":"section","text":"A Julia throw inside a --trim-compiled @ccallable does not become a Python exception — at best it aborts the host process. JuliaLibWrapping defines a small convention for surfacing errors across the C ABI so that generated wrappers can translate them into native exceptions."},{"title":"The JLWStatus convention","page":"Error handling","location":"error_handling/#The-JLWStatus-convention","category":"section","text":"The canonical status struct is defined in the JLWInterop subdir package and has the following shape:\n\nstruct JLWStatus\n    code::Int32                       # 0 == success; non-zero == error\n    message::NTuple{256, UInt8}       # UTF-8, null-terminated\nend\n\nA library opts in by either\n\nreturning a JLWStatus directly, or\nreturning a struct that contains a JLWStatus field (any field name).\n\nEither form is recognized by the Python backend.\n\nThe fixed 256-byte message buffer is deliberate: a Cstring or Ptr{UInt8} would raise an ownership question (who frees it, when?) that has no good answer under juliac --trim. An inline buffer keeps JLWStatus an isbits type with no heap allocation, at the cost of a bounded message length."},{"title":"Authoring a library","page":"Error handling","location":"error_handling/#Authoring-a-library","category":"section","text":"Add JLWInterop to your library's project, then construct status values with the provided helpers:\n\nusing JLWInterop\n\nstruct ResultStruct\n    status::JLWStatus\n    value::Float64\nend\n\nBase.@ccallable function compute(x::Float64)::ResultStruct\n    x < 0 && return ResultStruct(jlw_error(1, \"negative input\"), 0.0)\n    return ResultStruct(jlw_ok(), x * 2)\nend\n\njlw_ok() and jlw_error(code, msg) perform no heap allocation, so they are safe in --trim builds."},{"title":"What the Python wrapper does","page":"Error handling","location":"error_handling/#What-the-Python-wrapper-does","category":"section","text":"For each entrypoint whose return type carries a JLWStatus (directly or via an embedded field), the generated wrapper checks status.code and raises a JLWError (a RuntimeError subclass, carrying .code and .message) when it is non-zero:\n\nfrom mylib import compute, JLWError\n\ntry:\n    result = compute(-1.0)\nexcept JLWError as e:\n    print(e.code, e.message)   # 1, \"negative input\"\n\nOn success the full return struct is handed back unchanged, including the status field (so callers can still inspect it explicitly if they prefer)."},{"title":"C backend","page":"Error handling","location":"error_handling/#C-backend","category":"section","text":"The C header generator does not currently auto-emit a status-check macro; C callers should check result.status.code themselves and read the message with e.g. printf(\"%.256s\\n\", result.status.message). A JLW_CHECK-style helper macro is a possible follow-up."},{"title":"Recognition is structural","page":"Error handling","location":"error_handling/#Recognition-is-structural","category":"section","text":"The Python emitter matches on struct name (\"JLWStatus\") plus field shape (code::Int32 followed by a 256-byte tuple message). Authors who copy and paste a compatible definition — rather than depending on JLWInterop — still get the same wrapper behavior. The canonical package merely keeps the definition from drifting across libraries."},{"title":"JuliaLibWrapping","page":"Home","location":"#JuliaLibWrapping","category":"section","text":"JuliaLibWrapping generates C headers and Python ctypes bindings for shared libraries compiled from Julia by juliac. It turns the ABI metadata that juliac emits into wrappers that let non-Julia programs call the compiled library as if it were any other native dependency."},{"title":"Who it is for","page":"Home","location":"#Who-it-is-for","category":"section","text":"Authors of Julia libraries who want to ship a compiled artifact that downstream users — typically C or Python programmers — can consume without installing a Julia runtime themselves. The compiled library is produced by juliac; this package produces the binding code that makes it usable from the target language."},{"title":"The two-tool split","page":"Home","location":"#The-two-tool-split","category":"section","text":"The pipeline is split deliberately across two packages:\n\njuliac / JuliaC.jl --emits--> JSON ABI-info file --consumed by--> JuliaLibWrapping --emits--> .h / Python package\n\nJuliaC.jl compiles the shared library and emits a JSON file describing its ABI, but does not generate wrappers. JuliaLibWrapping consumes that JSON and emits the wrappers. Coupling between the two repos is the JSON format alone.\n\nbuild_library runs both halves in one call when JuliaC.jl is loaded; see Concepts for the pipeline architecture and the bundling, multi-library, and two-tier output stories."},{"title":"Where to go next","page":"Home","location":"#Where-to-go-next","category":"section","text":"Concepts — the pipeline, the ABI data model, the extension point for new target languages, and the runtime-closure / bundling story.\nJLWInterop — the dependency-free vocabulary package (CVector, CMatrix, CString, JLWStatus) that compiled libraries and the Python emitter both speak.\nError handling across the ABI — the JLWStatus convention that lets wrapped libraries surface errors as native exceptions in the target language.\nAPI reference — generated docstrings for the public API."}]
}
